# ovim Issue Tracker

| ID | Status | Priority | Complexity | Description |
|----------|---------|----------|------------|-------------|
| OV-00271 | Done | HIGH | Medium | [WRAP] Cursor (and mouse clicks) misplaced by `scroll_subrow` rows when the viewport is scrolled partway into a wrapped line — a related bug to OV-00019 (which only fixed the `textwidth` width-mismatch). The buffer renderer skips `scroll_subrow` wrapped rows of the top logical line, but the hardware-cursor and overlay screen-row math computed the viewport origin as `logical_to_visual(scroll_offset)` only, drawing the cursor `scroll_subrow` rows too *low* (cursor below input point — exactly OV-00019's symptom, common whenever many wrapped rows precede the cursor). The inverse screen→buffer mapping (mouse clicks) had the same omission, landing clicks `scroll_subrow` rows too *high*. Fixed by a single source of truth `WrapMap::viewport_top_visual_row(scroll_offset, scroll_subrow)` used by all cursor sites (core.rs), overlay positioning (overlays.rs ×2), and the 5 click/link mappings (mouse.rs), plus the test viewport helper. Regression tests: `cursor_screen_position_tests` (real render → hardware cursor row) and `wrap_click_scroll_test` (click resolves correct line); both proven to fail pre-fix. |
| OV-00003 | Pending | MEDIUM | Low | [MCP] `symbols` command fails with "Unknown tool: get_symbols" - should fall back to standard LSP textDocument/documentSymbol if MCP tool unavailable (cli/symbols.rs) |
| OV-00004 | Pending | MEDIUM | Low | [MCP] `diagnostics` command fails with "Unknown tool: get_diagnostics" - should fall back to standard LSP textDocument/publishDiagnostics cache (cli/diagnostics.rs) |
| OV-00005 | Pending | LOW | Low | [STATE] `snapshot` returns null for cursor_line, cursor_col, file fields - state not being serialized correctly (cli/snapshot.rs) |
| OV-00006 | Resolved | TRIAGE | Medium | [LSP] `hover` returns null on valid symbol positions — Resolved by OV-00164: `ensure_lsp_document_synced` now flushes pending changes via `flush_pending_changes_broadcast` directly instead of feeding the debouncer, so hover requests run against current server-side content. Verified end-to-end against rust-analyzer at `/tmp/ov6_repro/src/lib.rs` line 9 col 12 (`Point::new` definition) and line 14 col 12 (`distance_squared` method): hover returns the expected signature + docs. |
| OV-00007 | Done | MEDIUM | Low | [LSP] `find-references` returns empty array — Fixed: the `ovim lsp references` CLI was sending `gr` keystrokes (which only arms the LSP `gr*` prefix) instead of `grr` (which actually fires `request_find_references`). Rewrote `cmd_find_references` to send `grr`, poll the snapshot for the picker (mirrors the `cmd_hover` pattern; ceiling 6 s), and dismiss the picker on the way out so a second invocation lands in NORMAL. Underlying LSP path (verified end-to-end) was correct after OV-00164. Coverage in `lsp_grr_keybinding_test.rs` pins both `gr` arming and `grr` firing the intent so the CLI contract can't drift silently again. (ovim/src/subcommands.rs) |
| OV-00015 | Pending | MEDIUM | Medium | [PERF] Incremental wrap map invalidation only covers cursor line — [details](issue-docs/OV-00015-incremental-invalidation-cursor-only.md). (src/editor/mod.rs:ensure_wrap_map) |
| OV-00016 | Pending | LOW | Medium | [WRAP] No virtcol/curswant tracking for gj/gk — [details](issue-docs/OV-00016-virtcol-curswant.md). (src/editor/input/normal/pending_commands.rs) |
| OV-00019 | Done | HIGH | N/A | When there is a wrapping line above the cursor in the current buffer and the textwidth option is set, there is a miscalculation causing the vertical cursor position to be incorrect (cursor is below input point). There seems to be other related bugs, so investigation so required. |
| OV-00023 | Pending | LOW | Low | [TEST] `bug_reproduction_test` segfaults (SIGSEGV signal 11) on both main and feature branches — likely mlua/LuaJIT FFI issue, pre-existing. |
| OV-00025 | Done | MEDIUM | Low | [CMD] Up/Down arrows in path completion don't update command line text — visual selection changes but command line stays the same, so Enter executes the Tab-selected entry, not the arrow-selected one. (src/editor/input/commands.rs) |
| OV-00046 | Done | HIGH | High | [ARCH] `commands.rs` duplication reduced — `:set` was already in cmd_set.rs, `:bn`/`:bnext` already in cmd_buffer.rs. Extracted save_buffer() (5 write variants → 1 helper) and reload_buffer()/edit_file() (4 edit variants → 2 helpers). File reduced from ~2005 to 1920 lines. Remaining length is genuine command variety, not duplication. (src/commands.rs) |
| OV-00047 | Done | MEDIUM | High | [ARCH] `lsp/server.rs` — 24 AtomicBool fields replaced with single AtomicU32 bitflags. Adding a capability now requires 1 change (add flag constant + match arm). ~290 lines of boilerplate eliminated. (src/lsp/server.rs) |
| OV-00049 | Pending | LOW | Low | [WINDOW] Window focus overlap preference is dead code — `focus_left/right/up/down` originally had overlap preference checks (`vertical_overlap > 0`) but `|| true` made them no-ops. Now removed. To restore overlap preference, `focus_directional` sort should weight overlapping candidates higher (e.g., multi-key sort: overlap then distance). |
| OV-00054 | Done | TRIAGE | Medium | [LSP] TypeScript diagnostics not appearing — Fixed: `publish_diagnostics` client capability was missing from `TextDocumentClientCapabilities`. `typescript-language-server` checks `Boolean(publishDiagnostics)` and sets `diagnosticsSupport = false` when it's `None`, suppressing all diagnostic notifications. Added `PublishDiagnosticsClientCapabilities` with `related_information`, `tag_support`, and `version_support`. (ovim-core/src/lsp/server.rs:761) |
| OV-00055 | Done | LOW | Low | [DEAD CODE] `Change::join_lines()` constructor has zero callers — superseded by `RepeatAction::JoinLines` + `Buffer::join_lines()`. The constructor (ovim-core/src/change.rs:269-285) and its match arms in `repeat()` and `undo_redo()` are dead code. (ovim-core/src/change.rs:269) |
| OV-00056 | Done | MEDIUM | Low | [PANIC] `wrap_map.as_mut().unwrap()` can panic if wrap_map is None — two unwrap calls on Option in `ensure_wrap_map`. If wrap_map initialization fails or is called before setup, these panic in production. Should use `if let Some(map)` or initialize defensively. (ovim-core/src/editor/mod.rs:714,720) |
| OV-00057 | Done | MEDIUM | Low | [LOGIC] Inconsistent cursor clamping between `validate_cursor_position()` and `clamp_cursor_col()` — `validate_cursor_position()` uses `col >= line_len` (clamps col 0 on 1-char line), while `clamp_cursor_col()` uses `col > 0 && col >= line_len` (preserves col 0). Both use grapheme_count. The `col > 0` guard in `clamp_cursor_col` is intentional (col 0 is always valid), so `validate_cursor_position` has the bug: it would clamp col 0 to col 0 on a 1-char line (no-op in practice, but semantically wrong — it enters the branch unnecessarily). (ovim-core/src/buffer/mod.rs:241, ovim-core/src/buffer/text_ops.rs:307) |
| OV-00058 | Done | HIGH | Medium | [FEATURE] Operator/motion gap: `b`, `e`, `B`, `E`, `h`, `0`, `^`, `W` now wired with d/c/y operators. All have RepeatAction variants for dot-repeat and undo. |
| OV-00059 | Pending | LOW | Low | [TEST] No unit tests for Buffer methods `toggle_char_at_cursor()`, `indent_lines_at()`, `dedent_lines_at()`, `clamp_cursor_col()` — these were extracted from Editor-level code and have integration test coverage via repeat_action_test and cursor_clamping_test, but no direct Buffer-level unit tests. (ovim-core/src/buffer/text_ops.rs) |
| OV-00060 | Pending | LOW | Low | [TEST] Missing `~` toggle case on empty line test — `toggle_char_at_cursor()` returns false on empty line but no test verifies this edge case. Also missing: undo/redo cycle tests for each RepeatAction variant (indent, dedent, toggle case, join). (ovim-core/src/buffer/text_ops.rs:325) |
| OV-00061 | Pending | MEDIUM | High | [ARCH] Large files remaining — editor/mod.rs (1952), lsp/server.rs (~1840), commands.rs (1920), lsp/requests.rs (1815), change.rs (1769), subcommands.rs (1766), renderer/buffer.rs (1743), input/commands.rs (1636), helpers.rs (1611), operators.rs (1523). motions.rs split into 10 modules (max 615 lines). commands.rs reduced from 2005 to 1920. lsp/server.rs reduced ~100 lines via bitflags. (multiple files) |
| OV-00062 | Pending | MEDIUM | High | [ARCH] Dual undo system mid-migration — Pattern A (`Change::insert/delete` + `add_change()`, 73 call sites) coexists with Pattern B (`record()` + `push_recorded_undo()` + `set_repeat_action()`, ~47 call sites). Pattern A is still needed for insert-mode undo grouping. The `Change` enum has dead variants (like `JoinLines`) from the migration. No clear boundary between which operations use which pattern. (ovim-core/src/change.rs, ovim-core/src/editor/change_tracking.rs) |
| OV-00063 | Done | HIGH | Medium | [UNDO] `pop_last_change` pops unrelated undo entry when delete phase produces no edits — Fixed: token-based undo stack safety (commit 5958688). |
| OV-00064 | Done | MEDIUM | Medium | [UNDO] save_point corrupted by pop_last_change — Fixed: token-based undo stack safety (commit 5958688). |
| OV-00065 | Done | LOW | Low | [UNDO] Missing validate_cursor_position in semantic change undo variants — Fixed: token-based undo stack safety (commit 5958688). |
| OV-00066 | Done | MEDIUM | Medium | [INDENT] expandtab setting now consulted by indent/dedent. |
| OV-00067 | Done | MEDIUM | Low | [INDENT] shiftwidth setting now used for indent operations. |
| OV-00068 | Done | LOW | Low | [INDENT] Empty lines now skipped during indent. |
| OV-00069 | Done | MEDIUM | Low | [INDENT] Cursor now positioned on start line at first non-blank after `>>`. |
| OV-00070 | Done | LOW | Low | [INDENT] Cursor now at first non-blank after `>>`. |
| OV-00071 | Done | LOW | Low | [INDENT] Cursor now moved to first non-blank after `<<`. |
| OV-00072 | Done | MEDIUM | Medium | [INDENT] Visual mode `=` now has undo support via `record()`. |
| OV-00073 | Done | LOW | Low | [INDENT] auto_indent_lines now uses char count for leading_len. |
| OV-00074 | Done | LOW | Low | [INDENT] Ctrl-T now respects expandtab setting. |
| OV-00075 | Done | HIGH | Medium | [SCROLL] `viewport_command_active` removed entirely (commit fbde4c2). |
| OV-00076 | Done | HIGH | Medium | [SCROLL] Ctrl-e/Ctrl-y now update buffer cursor (commit fbde4c2). |
| OV-00077 | Done | MEDIUM | Medium | [SCROLL] update_scroll_offset now uses focused window height (commit fbde4c2). |
| OV-00078 | Done | MEDIUM | Low | [SCROLL] sidescrolloff now clamped (commit fbde4c2). |
| OV-00079 | Done | MEDIUM | Low | [SCROLL] N/A — viewport_command_active removed entirely (commit fbde4c2). |
| OV-00080 | Done | HIGH | Low | [MOTION] `G` now clamped with `.min(max_line)`. |
| OV-00081 | Done | HIGH | Low | [MOTION] `gg` now clamped with `.min(max_line)`. |
| OV-00082 | Done | MEDIUM | Medium | [MOTION] `b` from col 0 now correct backward walk. |
| OV-00083 | Done | MEDIUM | Low | [MOTION] `$` now handles count (count-1 lines down). |
| OV-00084 | Done | LOW | Low | [MOTION] `_` now handles `count > 1`. |
| OV-00085 | Done | LOW | Low | [MOTION] `+` now uses `buffer.line_count()`. |
| OV-00086 | Done | MEDIUM | Medium | [MOTION] `%` doesn't search forward for bracket when cursor is not on one — Vim searches forward on current line for nearest bracket, then jumps to its match. ovim only works if cursor is already on a bracket. Also matches `<`/`>` which Vim doesn't. (ovim-core/src/editor/motions.rs:734-746) |
| OV-00087 | Done | MEDIUM | Low | [MOTION] `t` now returns false for no movement (`i - 1 > col`). |
| OV-00088 | Done | MEDIUM | Low | [MOTION] `]}` now skips cursor with `abs_pos + 1`. |
| OV-00089 | Done | MEDIUM | Medium | [MOTION] `ge` now has correct backward algorithm. |
| OV-00090 | Done | MEDIUM | Low | [PASTE] p/P count now implemented via `text.repeat(count)`. |
| OV-00091 | Done | MEDIUM | Low | [PASTE] P cursor now correctly positioned (`end_pos.1 - 1`). |
| OV-00092 | Done | MEDIUM | Medium | [PASTE] Visual paste now updates unnamed register. |
| OV-00093 | Done | MEDIUM | Medium | [PASTE] Visual-line now uses `paste_before`. |
| OV-00094 | Done | MEDIUM | Medium | [REGISTER] `delete_history` now `Vec<RegisterContent>`. |
| OV-00095 | Done | LOW | Low | [REGISTER] Named register ops now update unnamed register. |
| OV-00096 | Done | LOW | Low | [REGISTER] Uppercase append now updates RegisterType. |
| OV-00097 | Done | LOW | Low | [PASTE] Empty buffer paste edge case fixed. |
| OV-00098 | Done | LOW | Low | [PASTE] Last line paste trailing newline fixed. |
| OV-00099 | Done | HIGH | Medium | [EDIT] `:e!` does not clear undo history on reload — Fixed: `reload_from_disk()` calls `reset_derived_state()` which replaces ChangeManager. (ovim-core/src/buffer/file_io.rs) |
| OV-00100 | Done | HIGH | Medium | [EDIT] `:e` (bare) does not clear undo history on reload — Fixed: same path as OV-00099, `:e` calls `reload_from_disk()`. (ovim-core/src/commands.rs) |
| OV-00101 | Done | HIGH | Low | [EDIT] `:e filename` does not check for unsaved changes — Fixed: commands.rs checks `editor.is_modified()` before loading. (ovim-core/src/commands.rs) |
| OV-00102 | Done | MEDIUM | Low | [EDIT] `:e! filename` (force-edit different file) not supported — Fixed: commands.rs handles `e! ` prefix with tilde expansion. (ovim-core/src/commands.rs) |
| OV-00103 | Done | MEDIUM | Low | [EDIT] `reload_from_disk` does not reset fold state — Fixed: `reset_derived_state()` calls `fold_manager.delete_all()`. (ovim-core/src/buffer/mod.rs) |
| OV-00104 | Done | MEDIUM | Low | [EDIT] `reload_from_disk` does not increment version counter — Fixed: `reset_derived_state()` increments `self.version`. (ovim-core/src/buffer/mod.rs) |
| OV-00105 | Done | MEDIUM | Medium | [EDIT] `reload_from_disk` does not notify LSP of content change — Fixed: added `mark_buffer_modified_force_send()` after reload in `:e`, `:e!`, and `:checktime` handlers. (ovim-core/src/commands.rs) |
| OV-00106 | Done | MEDIUM | Low | [EDIT] Cursor clamping in `reload_from_disk` includes newline char — Fixed: uses `line_count()` + `validate_cursor_position()`. (ovim-core/src/buffer/file_io.rs) |
| OV-00107 | Done | MEDIUM | Medium | [EDIT] `:checktime` reload doesn't reset undo/cursor/folds — Fixed: `reload_if_changed()` calls `reset_derived_state()` + `validate_cursor_position()`. (ovim-core/src/buffer/file_io.rs) |
| OV-00108 | Done | LOW | Low | [EDIT] `reload_from_disk` does not reset git status/blame — Fixed: `reset_derived_state()` resets `git_status` and `git_blame`. (ovim-core/src/buffer/mod.rs) |
| OV-00109 | Done | LOW | Low | [EDIT] `reload_from_disk` does not clear highlight/cache state — Fixed: `reset_derived_state()` clears all highlight/cache fields. (ovim-core/src/buffer/mod.rs) |
| OV-00110 | Done | LOW | Low | [DEAD CODE] Duplicate `:e filename` handler in input/commands.rs — Fixed: confirmed no `:e` handler exists in input/commands.rs. (ovim-core/src/editor/input/commands.rs) |
| OV-00111 | Done | CRITICAL | Medium | [LSP] Incremental sync uses char count instead of UTF-16 code units — Fixed: extracted `char_col_to_utf16`/`utf16_to_char_col` into `lsp/position.rs`, updated `compute_simple_diff` to use UTF-16 positions. (ovim-core/src/lsp/position.rs, utils.rs) |
| OV-00112 | Done | HIGH | Low | [LSP] All response parsers silently swallow deserialization errors — Fixed: created `parse_lsp_response<T>()` helper that logs parse failures, replaced all 25 `.ok()` sites. (ovim-core/src/lsp/requests.rs) |
| OV-00113 | Done | HIGH | Medium | [LSP] Pending request leaked on early return after HashMap insertion — Fixed: reordered `request()` to insert pending entry AFTER precondition checks and channel send. (ovim-core/src/lsp/server.rs) |
| OV-00114 | Done | HIGH | Medium | [LSP] Reader now loops over headers until empty line, correctly handling multi-header responses per LSP spec. |
| OV-00115 | Done | HIGH | Medium | [LSP] Cancellation `.take()` silently drops pending responses of other variant types — Fixed: replaced `PendingLspResponse` enum with `PendingLspResponses` struct with typed Option fields per request type. (ovim-core/src/editor/lsp_state.rs, hover.rs, goto.rs, lsp_integration.rs) |
| OV-00116 | Done | HIGH | Low | [LSP] `send_lsp_changes_if_modified` can send didChange before didOpen — Fixed: added `did_open_sent` guard that returns early. (ovim-core/src/editor/lsp_integration.rs) |
| OV-00117 | Done | HIGH | Low | [LSP] `update_diagnostics` assigns cached count to itself — Fixed: compute count directly from fetched diagnostics instead of self-assignment. (ovim-core/src/editor/lsp_modules/diagnostics.rs) |
| OV-00118 | Done | HIGH | Low | [LSP] `expect()` panics reachable from user input in document symbols picker — Fixed: replaced `.expect()` with graceful error handling that returns early on scratch buffers. (ovim-core/src/editor/lsp_modules/references.rs) |
| OV-00119 | Won't Fix | HIGH | Medium | [LSP] Formatting edits returned unsorted — `apply_lsp_edits()` already sorts edits back-to-front before applying. No change needed. (ovim-core/src/lsp/requests.rs) |
| OV-00120 | Done | HIGH | Medium | [LSP] `did_save_broadcast` uses non-broadcast flush — Fixed: changed to `flush_pending_changes_broadcast`. (ovim-core/src/lsp/notifications.rs) |
| OV-00121 | Done | HIGH | Medium | [LSP] Broadcast flush increments version once per server — Fixed: increment version once before loop, use `send_did_change_with_version` for all servers. (ovim-core/src/lsp/notifications.rs) |
| OV-00122 | Done | HIGH | Medium | [LSP] start_server TOCTOU race — Fixed: added `starting_servers: DashSet<String>` guard to prevent concurrent initialization. (ovim-core/src/lsp/mod.rs) |
| OV-00123 | Done | HIGH | Low | [LSP] Supervisor backoff counter never resets — Fixed: reset `restarts = 0` after healthy runs (>60s uptime). (ovim-core/src/lsp/supervisor.rs) |
| OV-00124 | Done | HIGH | Low | [LSP] Server-initiated requests dropped when notification channel full — Fixed: enhanced backpressure logging for all dropped server-initiated requests. (ovim-core/src/lsp/notifications.rs) |
| OV-00125 | Done | MEDIUM | Low | [LSP] `shutdown()` does not drain pending_requests — Fixed: drain all pending requests with error before shutdown. (ovim-core/src/lsp/server.rs) |
| OV-00126 | Done | MEDIUM | Low | [LSP] Windows line endings cause off-by-one in incremental diff — Fixed: strip `\r` from lines after splitting. (ovim-core/src/lsp/utils.rs) |
| OV-00127 | Done | MEDIUM | Low | [LSP] Rename now handles documentChanges (workspace_edits.rs:56-130). |
| OV-00128 | Done | MEDIUM | Low | [LSP] No guard against double didOpen for same URI — Fixed: check `document_versions` before sending; log warning and return on duplicate. (ovim-core/src/lsp/notifications.rs) |
| OV-00129 | Won't Fix | MEDIUM | Low | [LSP] `workspace/applyEdit` responds success before edit is applied — By design, matches VS Code behavior of optimistic response. (ovim-core/src/lsp/notifications.rs) |
| OV-00130 | Done | MEDIUM | Low | [LSP] Notification listener JoinHandle dropped, task not cancellable — Fixed: store handle in `listener_handles: DashMap`, abort on `stop_server`. (ovim-core/src/lsp/mod.rs, notifications.rs) |
| OV-00131 | Done | MEDIUM | Low | [LSP] Diagnostics not cleaned up when server is stopped — Fixed: `stop_server()` now removes diagnostics per server and sets `diagnostics_changed` flag. (ovim-core/src/lsp/mod.rs) |
| OV-00132 | Blocked | MEDIUM | Low | [LSP] `type_hierarchy` capability hardcoded to false — Blocked on lsp-types upgrade. Added TODO comment. (ovim-core/src/lsp/server.rs) |
| OV-00133 | Done | MEDIUM | Low | [LSP] `set_capability_by_method` maps wrong method for executeCommand — Fixed: corrected to `workspace/executeCommand`. (ovim-core/src/lsp/server.rs) |
| OV-00134 | Done | MEDIUM | Low | [LSP] Diagnostic column comparison skips UTF-16 conversion — Fixed: use `utf16_to_char_col` for comparison. (ovim-core/src/editor/lsp_modules/diagnostics.rs) |
| OV-00135 | Done | MEDIUM | Low | [LSP] Supervisor "exponential" backoff is actually linear — Fixed: use `initial_backoff * 2^(restarts-1)` for true exponential backoff. (ovim-core/src/lsp/supervisor.rs) |
| OV-00136 | Done | LOW | Low | [LSP] LSP error code -32801 (ContentModified) not handled specially — Fixed: handle alongside -32800 (Cancelled) with debug log only. (ovim-core/src/lsp/server.rs) |
| OV-00137 | Done | LOW | Low | [LSP] `clear_lsp_state` does not clear hover_cache or abort pending responses — Fixed: abort all pending response tasks and clear `hover_cache` in `clear_lsp_state`. (ovim-core/src/editor/lsp_integration.rs) |
| OV-00138 | Done | MEDIUM | High | [UNICODE] Grapheme vs char column systemic inconsistency — Fixed via Hybrid approach (Option C): documented cursor.col() as grapheme index, added `grapheme_to_char_col()` and `char_to_grapheme_col()` conversion helpers in unicode module, and inserted conversions at rope operation boundaries. Fixed callsites: `toggle_char_at_cursor`, `join_lines_impl` junction_col, `modify_number_at_cursor`, `word_under_cursor`, search forward/backward/gn/gN column handling, case change motion/to-end-of-line, search_manager line length comparisons. Added 20 grapheme-specific integration tests (ZWJ emoji, flags, skin tones, combining marks). Remaining char-based sites (highlighting, text objects, visual mode) are lower priority and tracked separately. (ovim-core/src/unicode/mod.rs, ovim-core/src/buffer/text_ops.rs, ovim-core/src/search.rs, ovim-core/src/editor/search_manager.rs, ovim-core/src/editor/input/case.rs) |
| OV-00139 | Done | LOW | Low | [MOTION] `section_forward()` and `section_end_forward()` use `rope.len_lines()` instead of `buffer.line_count()` — Fixed: both now use `buffer.line_count()`. (ovim-core/src/editor/motions.rs) |
| OV-00140 | Done | LOW | Low | [VISUAL] `handle_yb` and `handle_y_big_b` missing `set_yank_flash_range()` — every other yank handler calls it but these two didn't, so yb/yB yanks had no visual flash feedback. Fixed: added flash calls. (ovim-core/src/editor/input/normal/operators.rs:1595,1630) |
| OV-00141 | Done | HIGH | Low | [LSP] Paste (p/P), toggle case (~), and other operations using `record()` directly never called `mark_buffer_modified()` — LSP `didChange` was never sent after paste, so diagnostics were never refreshed. Fixed: `push_recorded_undo()` now calls `mark_buffer_modified()` automatically. Also: `mark_buffer_modified()` now clears stale cached diagnostics to prevent wrong-line rendering. (ovim-core/src/editor/change_tracking.rs:135, ovim-core/src/editor/lsp_integration.rs:689) |
| OV-00142 | Done | HIGH | Low | [LSP] `repeat_last_change()` (dot-repeat) never called `mark_buffer_modified()` — both RepeatAction and Change-based paths pushed directly to undo stack, bypassing LSP notification. Dot-repeating any edit wouldn't trigger `didChange`. Fixed: added `self.mark_buffer_modified()` after both paths. (ovim-core/src/editor/change_tracking.rs:90,120) |
| OV-00143 | Done | MEDIUM | Low | [LSP] `accept_completion()` never called `mark_buffer_modified()` — accepting an LSP completion modified the buffer but the server was never notified via `didChange`. Fixed: added `mark_buffer_modified()` after `add_change()`. (ovim-core/src/editor/ui_features.rs:105) |
| OV-00144 | Done | MEDIUM | Low | [LSP] `confirm_substitute()` (interactive `:s///c`) never called `mark_buffer_modified()` — confirming a substitution modified the buffer but no `didChange` was sent. Fixed: added `mark_buffer_modified()` after `add_change()`. (ovim-core/src/editor/ui_features.rs:373) |
| OV-00145 | Done | MEDIUM | Low | [LSP] Visual block insert replay happened before `mark_buffer_modified()` in `exit_insert_mode()` — the first line's change triggered `didChange` but remaining lines' insertions were invisible to LSP. Fixed: moved `mark_buffer_modified()` after visual block replay. (ovim-core/src/editor/input/insert_mode.rs:262) |
| OV-00146 | Done | MEDIUM | Low | [NAV] Diagnostic navigation (`]d`/`[d`) hardcoded column 0 — `goto_next_diagnostic()` and `goto_prev_diagnostic()` extracted only `d.range.start.line` and ignored `d.range.start.character`. Fixed: extract both line and character, convert UTF-16 to char column via `utf16_to_col()`, compare position by (line, col) for same-line diagnostics. (ovim-core/src/editor/mod.rs:1688,1715) |
| OV-00147 | Done | MEDIUM | Low | [NAV] Live grep results hardcoded column 0 — `spawn_grep_search()` set `col: 0` in all `PickerResult`s. Fixed: use `matcher.find()` to get match byte offset within the line, convert to char column. Display format now includes column (`file:line:col`). (ovim-core/src/editor/grep.rs:131) |
| OV-00148 | Done | LOW | Low | [NAV] `PickerAction::OpenFile` missing `validate_cursor_position()` and `center_cursor_in_viewport()` — cursor could land on invalid position and viewport wouldn't scroll to show the match. `OpenFileWithTag` already had both calls. Fixed: added both after `set_position()`. (ovim-core/src/editor/picker_manager.rs:506) |
| OV-00149 | Done | HIGH | Medium | [LSP] Debounce timer flushes to single server, not broadcast — Fixed: `process_flush_requests()` now calls `flush_pending_changes_broadcast()` with language_id, broadcasting to all servers. (ovim-core/src/lsp/notifications.rs:1111) |
| OV-00150 | Done | HIGH | Medium | [PERF] Version lock held across `.await` in `send_did_change_immediate` — Fixed: lock is now acquired, version incremented, params built, then lock dropped before I/O. Both `send_did_change_immediate` and `flush_pending_changes_broadcast` follow this pattern. (ovim-core/src/lsp/notifications.rs:192-210) |
| OV-00151 | Done | HIGH | Medium | [PERF] `merge_diagnostics()` called 2-3x per render frame under Mutex — Fixed: added `merged_diagnostics_cache` that is checked first; cache invalidated on `set_diagnostics` and `stop_server`. (ovim-core/src/lsp/mod.rs:432-512) |
| OV-00152 | Done | HIGH | Low | [PERF] Full document content cloned into debouncer on every keystroke — Fixed: changed `ChangeDebouncer.pending_text` and `old_text` from `String` to `Arc<str>`. Also changed `DocumentSyncState.last_flushed_content` and `last_queued_content` to `Arc<str>`. Clones during debounce are now cheap Arc refcount bumps. Full String allocation only happens once at flush time. (ovim-core/src/lsp/mod.rs, notifications.rs, lsp_state.rs, lsp_integration.rs) |
| OV-00153 | Done | MEDIUM | Low | [LSP] 3 request methods bypass `parse_lsp_response` helper — Fixed: `document_symbols` and `workspace_symbols` already had logging added for both parse paths. `inlay_hints` already uses `parse_lsp_response`. `semantic_tokens_full` and `semantic_tokens_range` now use `parse_lsp_response` instead of bare `from_value`. (ovim-core/src/lsp/requests.rs) |
| OV-00154 | Done | MEDIUM | Low | [LSP] Health check uses `child.id()` instead of `try_wait()` — Fixed: now uses `child.try_wait()` to detect exited/zombie processes. (ovim-core/src/lsp/server.rs:1460-1472) |
| OV-00155 | Done | MEDIUM | Low | [LSP] Initialize timeout leaves orphaned pending request — Fixed: `initialize()` now catches timeout and transitions to `Failed` state, triggering cleanup of orphaned pending requests. (ovim-core/src/lsp/server.rs:687-716) |
| OV-00156 | Done | MEDIUM | Low | [LSP] `ShuttingDown` state not rejected in request gate — Fixed: `request()` now explicitly rejects `ShuttingDown` state with immediate error. (ovim-core/src/lsp/server.rs:1185-1189) |
| OV-00157 | Done | MEDIUM | Low | [LSP] `pending_completion` not cleared on buffer switch — Fixed: `clear_lsp_state()` now takes and aborts `pending_completion`. (ovim-core/src/editor/lsp_integration.rs:683-685) |
| OV-00158 | Pending | LOW | Low | [PERF] Double serialization for message size validation — `request()` serializes message to check size against MAX_MESSAGE_SIZE, then `write_message()` serializes again for transmission. Every LSP request/notification is serialized twice. Microsecond overhead; fixing requires changing channel type from `JsonRpcMessage` to pre-serialized bytes. (ovim-core/src/lsp/server.rs:1170-1186) |
| OV-00159 | Pending | LOW | Low | [LSP] Capability flags use `Ordering::Relaxed` — all 24 AtomicBool capability caches use Relaxed ordering. No happens-before guarantee between init task writing and editor task reading. Safe on x86-64 but could cause stale reads on ARM. (ovim-core/src/lsp/server.rs:1456-1627) |
| OV-00160 | Pending | LOW | Low | [LSP] No state transition validation in server state machine — `transition_to()` accepts any state regardless of current state. Invalid transitions (Ready→Initializing, Terminated→Ready) are not prevented. (ovim-core/src/lsp/server.rs:947-970) |
| OV-00161 | Done | HIGH | Medium | [LSP] `update_diagnostic_cache` stamps old diagnostics with current buffer version — Already addressed: `spawn_diagnostic_cache_refresh()` now detects buffer version drift at poll time (line 189) and re-requests fresh diagnostics. Stale diagnostics are shown temporarily (better UX than blank) until the refresh completes. The `diagnostics_cache_stale()` check intentionally only validates file path. (ovim-core/src/editor/lsp_modules/diagnostics.rs:87,189) |
| OV-00162 | Done | HIGH | Medium | [LSP] Unversioned `publishDiagnostics` bypass ALL staleness checks — `set_diagnostics()` only checks version when `version.is_some()`. Servers that omit the optional `version` field (common per LSP spec) have all diagnostics accepted unconditionally, even stale ones computed against old document content. Combined with OV-00161, stale diagnostics are both stored AND shown. (ovim-core/src/lsp/mod.rs:630-644) |
| OV-00163 | Done | HIGH | Medium | [LSP] LSP document version not bumped until debounce timer fires — Already addressed: `did_change()` now bumps `document_versions` immediately (line 266-271) before starting the debounce timer. `set_diagnostics()` version check at line 754 correctly rejects diagnostics with version < current. Comment at line 740-744 documents the fix. (ovim-core/src/lsp/notifications.rs:264-271, ovim-core/src/lsp/mod.rs:740-763) |
| OV-00164 | Resolved | HIGH | Medium | [LSP] `ensure_lsp_document_synced` feeds debouncer instead of flushing — Comment says "flush pending changes" but it calls `did_change_broadcast()` which starts a NEW 150ms debounce timer via `did_change()`. The 2ms+10ms sleep after is nowhere near enough. Hover/goto-definition requests are made against stale server-side content. Should call `flush_pending_changes_broadcast()` directly. (ovim-core/src/editor/lsp_integration.rs:984-1071, ovim-core/src/lsp/notifications.rs:510-522) |
| OV-00165 | Done | MEDIUM | Low | [LSP] Double debounce: editor-side 150ms + LspManager 150ms = 300ms+ latency — `should_send_change()` in `DocumentSyncState` debounces at 150ms before calling `did_change_broadcast()`, which starts ANOTHER 150ms timer in `ChangeDebouncer`. Combined worst case: 300ms+ from keystroke to `didChange` reaching server. One layer should be removed. (ovim-core/src/editor/lsp_state.rs:45-50, ovim-core/src/lsp/notifications.rs:350-402) |
| OV-00166 | Pending | LOW | Low | [LSP] `did_close` retains diagnostics, stale on reopen — `did_close` intentionally keeps diagnostics ("should remain visible even after file is closed"). If the file is modified externally and re-opened, old diagnostics at wrong positions are shown until the server sends fresh ones. (ovim-core/src/lsp/notifications.rs:452) |
| OV-00167 | Done | MEDIUM | Low | [MOTION] `method_end_forward`/`method_end_backward` use `line.find('}')` which returns byte offset, not char/grapheme offset — Fixed: replaced `str::find()` (byte offset) with `chars().position()` (char index) + `char_to_grapheme_col()` for correct grapheme-based cursor positioning. (ovim-core/src/editor/motions.rs:1885,1912) |
| OV-00168 | Pending | LOW | Low | [MOTION] `is_method_start` uses `line.len()` (bytes) for indent calculation — `let indent = line.len() - line.trim_start().len()` computes byte difference, compared against threshold of 8. Multi-byte whitespace (full-width spaces) would produce wrong indent count. Unlikely in practice since indentation is near-universally ASCII. (ovim-core/src/editor/motions.rs:1943) |
| OV-00169 | Pending | MEDIUM | Low | [MOTION] Sentence motions include trailing `\n` in chars vec — `let chars: Vec<char> = line.chars().collect()` includes newline from `buffer.line()`. Whitespace-skipping logic treats `\n` as whitespace, works by accident. `sentence_backward_once` inconsistently strips `\n` in some places (col clamping) but not others (chars vec). Makes bounds reasoning fragile and could cause bugs if future changes add character access after whitespace skip. (ovim-core/src/editor/motions.rs:1084,1160) |
| OV-00170 | Pending | LOW | Low | [EDIT] ~20 delete operations in text_ops.rs use `clamp_cursor_col` instead of `validate_cursor_position` — `clamp_cursor_col` only clamps column, not line. Most are safe (single-line deletes), but `delete_to_matching_bracket` (line 875) could theoretically leave cursor line past EOF on bracket pairs spanning entire buffer. Systemic safety net in `handle_key_event_internal` catches these, so not actively dangerous. (ovim-core/src/buffer/text_ops.rs:650,665,678,695,719,875) |
| OV-00171 | Pending | LOW | Medium | [MOTION] `is_method_start` heuristic produces false positives — the fallback C/Java pattern accepts any line with indent ≤ 8, a `(`, not starting with control keywords, ending with `{`. Matches non-function lines like `let x = foo({`. Tree-sitter queries for function nodes could replace heuristics for languages with grammar support. (ovim-core/src/editor/motions.rs:1941-1959) |
| OV-00172 | Pending | LOW | Low | [INPUT] Insert mode Esc handler doesn't explicitly move cursor back by one — Vim moves cursor left on Esc from insert mode (onto last typed character). The `validate_cursor_position` safety net in `handle_key_event_internal` now masks this, but the Esc handler itself is independently incorrect. Revealed by `test_a_empty_line` which previously asserted col 4 (past end) on 4-char line. (ovim-core/src/editor/input/insert_mode.rs) |
| OV-00173 | Done | CRITICAL | Medium | [PERF] O(n²) nested loop in `highlights_for_all_lines` — for each tree-sitter capture, linearly scans ALL lines to find overlaps. On a 5000-line file with 500 captures this is 2.5M iterations. Should use binary search on `line_start_bytes` to find affected line range. (ovim-core/src/syntax/highlighter.rs:98-121) |
| OV-00174 | Done | CRITICAL | Medium | [PERF] Per-character multiple linear scans in `render_line_with_highlights` — for EACH character in a line, scans search_matches, syntax_highlights, diagnostics, and control_ranges arrays. On a 200-char line with 50 highlights + 10 diagnostics = 15,000 iterations. These scans are then REPEATED in the `end_col` lookahead loop (lines 1897-1918), doubling the cost. Should pre-compute interval boundaries or use sorted merge. (ovim/src/ui/renderer/buffer.rs:1841-1918) |
| OV-00175 | Done | CRITICAL | Medium | [PERF] Full buffer serialization on every snapshot/buffer API call — `buffer.rope().to_string()` copies entire file into a String for every `/v1/buffer` or `/v1/snapshot` request. For large files this blocks the event loop with huge allocations. No range-based or streaming option exists. (ovim/src/event_loop.rs:1445-1456) |
| OV-00176 | Done | HIGH | Low | [PERF] Full frame background clear on every render — Fixed: removed blank-screen paragraph rendering from `clear_frame` (now `init_frame`). Ratatui's double-buffer diff handles clearing stale cells. Eliminates `" ".repeat(width) × height` allocation every frame. (ovim/src/ui/renderer/core.rs) |
| OV-00177 | Done | HIGH | Medium | [PERF] Per-line byte offset O(n) recalculation in `highlights_for_line` — Fixed: replaced `source.lines().enumerate()` scan with direct newline byte scanning. Note: main render path uses `cached_highlights` (O(1)) not this function; the picker preview path already caches per-line. (ovim-core/src/syntax/highlighter.rs) |
| OV-00178 | Done | HIGH | Low | [PERF] `block_in_place` + `block_on` in API handlers stalls async executor — Fixed: replaced `block_in_place(block_on(...))` with direct `.await` calls in the already-async `handle_api_request` function for both GetLspStatus and GetHealth handlers. (ovim/src/event_loop.rs) |
| OV-00179 | Done | HIGH | Low | [PERF] Unbounded channels for API requests and Java LSP status — Fixed: replaced `mpsc::unbounded_channel()` with bounded `mpsc::channel(256)` for API requests and `mpsc::channel(64)` for Java LSP status. All senders updated to use `.send().await` (async handlers) or `.try_send()` (sync contexts). (ovim/src/main.rs, ovim/src/api/, ovim/src/event_loop.rs, ovim/src/lsp_init/java.rs) |
| OV-00180 | Done | HIGH | Medium | [PERF] Highlight double-mapping allocates two Vec per line per frame — Fixed: introduced `HighlightShiftBuffers` struct with reusable `Vec<usize>` buffers passed into `shift_highlights_for_viewport`. Buffers are allocated once per render pass and reused across all visible lines. (ovim/src/ui/renderer/buffer.rs) |
| OV-00181 | Done | HIGH | Low | [PERF] ANSI render now cached per (width, height, plain) and invalidated on `mark_dirty()` via a new `render_input_version` counter. Idle-editor `/v1/render` polls hit the cache instead of re-running the full ratatui+highlight pipeline, so the main event loop stays responsive to other API requests / LSP polls. (ovim/src/ui/ansi.rs `AnsiRenderCache`) |
| OV-00182 | Done | HIGH | Low | [PERF] Line cache clear-all eviction destroys working set — Fixed: replaced `entries.clear()` with proximity-based eviction that retains entries within ±512 lines of the current viewport, preserving the working set across frames. (ovim/src/ui/renderer/line_cache.rs) |
| OV-00183 | Done | MEDIUM | Low | [PERF] MCP hover handler polls 10× with 100ms sleep — Fixed: dropped the polling loop in favour of a single `GetSnapshotLight` after `SendKeys("K")`. The event loop already blocks `SendKeys` on `has_pending_hover()` (up to 5s) so the hover is resolved by the time control returns; polling again just burned dead time on misses. Handler now also guards on `mode.contains("HOVER")` so stale `hover_info` from a prior K can't leak through. (ovim/src/api/mcp_handler.rs:198-259) |
| OV-00184 | Pending | MEDIUM | Low | [PERF] Path canonicalization via filesystem I/O in `find_buffer_by_path` loop — canonicalizes every buffer's path (filesystem syscall) on each call. Should cache canonicalized paths in a HashMap. (ovim-core/src/editor/buffer_manager.rs:230-243) |
| OV-00185 | Done | MEDIUM | Low | [PERF] Cursor style and terminal title set on every render frame — Fixed: `Renderer` now tracks `last_cursor_style` and `last_title`; `crossterm::execute!` only emitted when values change. (ovim/src/ui/renderer/core.rs) |
| OV-00186 | Pending | MEDIUM | Low | [PERF] File picker drain budget only 2ms — `file_rx.try_recv()` loop has 2ms budget to drain results. If file search produces thousands of results rapidly, most are deferred to future frames causing visible lag. (ovim/src/event_loop.rs:213-230) |
| OV-00187 | Pending | MEDIUM | Low | [PERF] Separator string allocation on every render — horizontal separator creates `sep_char.to_string().repeat(width)` and vertical separator calls `sep_char.to_string()` per line, all on every frame even when separators haven't changed. (ovim/src/ui/renderer/core.rs:743-769) |
| OV-00188 | Pending | LOW | Low | [PERF] Highlight sorts may be unnecessary — `highlights.sort_by_key(|(range, _)| range.start)` after collection in 3 places. Tree-sitter captures may already be ordered; if so, these sorts are wasted work. (ovim-core/src/syntax/highlighter.rs:125-127, 212-214, 272-273) |
| OV-00189 | Done | HIGH | Low | [MARK] Global mark jump uses `chars().count()` instead of `grapheme_count()` for column clamping — `jump_to_mark()` line 84 and `pop_tag_stack()` line 317 use `chars().count()` to measure line length, but cursor.col() is a grapheme index. With multi-codepoint characters (emoji, combining marks), cursor lands at wrong column. Also: line 88 clamps to `line_len` instead of `line_len.saturating_sub(1)`, allowing cursor one past end of line in normal mode. (ovim-core/src/editor/mark_jump.rs:84,88,317) |
| OV-00190 | Done | MEDIUM | Low | [MARK] Local mark jump (`'a`) doesn't validate mark.line against buffer bounds — Fixed: both local-mark branches in `jump_to_mark` and `jump_to_mark_line` now clamp `mark.line` to `line_count - 1` and the column to the line's last grapheme, matching the global-mark branch. Extracted shared `clamp_grapheme_col` helper and reused it in `tag_pop` to deduplicate. The `handle_key_event` post-input safety net previously masked this for keystroke-driven jumps, but any direct `editor.jump_to_mark` caller (LSP-driven jumps, scripted automation) would still corrupt cursor state — now contractually safe. Coverage in `mark_test.rs` (test_ov00190_*). (ovim-core/src/editor/mark_jump.rs) |
| OV-00191 | Done | LOW | Low | [WRAP] `invalidate_line` can theoretically underflow via `isize` cast — `(self.visual_offsets[i] as isize + diff) as usize` at line 137 wraps to a huge number if `diff` is negative and `visual_offsets[i]` is small. In practice `visual_offsets[i]` is always >= `old_count` so the result stays non-negative, but no runtime guard exists. (ovim-core/src/editor/wrap_map.rs:134,137) **Fixed:** moot — `invalidate_line` was deleted entirely as dead code (see OV-00263). |
| OV-00192 | Done | MEDIUM | Low | [FORMAT] `format_document` hardcodes tab_size=4 and insert_spaces=true — ignores user's `tabstop`, `shiftwidth`, and `expandtab` settings. LSP formatting uses these wrong defaults for all languages. (ovim-core/src/editor/lsp_modules/actions.rs:108-109) |
| OV-00193 | Done | MEDIUM | Low | [HIGHLIGHT] `update_highlights_for_insert/delete` uses `chars().count()` not `grapheme_count()` — highlight cache shift calculations at lines 177 and 194 use char count for multi-codepoint text, causing highlight ranges to drift on lines with emoji or combining characters. This issue restates existing TODO notes in code and is kept for tracking. (ovim-core/src/buffer/highlighting.rs:177,194,232,249) |
| OV-00194 | Won't Fix | N/A | N/A | [CONCURRENCY] Nested mutex locks in `set_diagnostics` — FALSE POSITIVE. Lock ordering is consistent: `document_versions` always acquired first, `last_sent_versions` second. No reverse ordering exists in the codebase. |
| OV-00195 | Won't Fix | N/A | N/A | [CONCURRENCY] Stdin write lock held across async I/O — BY DESIGN. The mutex serializes writes to the LSP stdin pipe. Releasing the lock between buffering and writing would allow interleaved bytes from concurrent messages. |
| OV-00196 | Won't Fix | N/A | N/A | [CONCURRENCY] Server-initiated requests dropped when channel full — KNOWN TRADEOFF. Deliberate `try_send()` to prevent deadlocks (comment: "BUG FIX"). Already logs dropped requests at line 1107. |
| OV-00197 | Done | LOW | Low | [ERROR] Lua config command failures silently discarded. **Fixed:** `run_config_command` (ovim-core/src/editor/lua_integration.rs) logs and toasts failures from init.lua and plugin command queues; `:source` reports failed command names in its status message (ovim-core/src/commands.rs). |

| OV-00198 | Done | LOW | Low | [ERROR] AI profile selection now reports unknown profiles at the shared `ai_set_profile` boundary, so keyboard, picker, slash-command, and mouse callers cannot fail silently. Empty profile lists also produce visible status. Regression coverage verifies a failed selection preserves the active profile and reports the missing name. (ovim-core/src/editor/ai_integration.rs) |
| OV-00199 | Duplicate | LOW | Low | [MOTION] Duplicate of OV-00169 — sentence motion trailing `\n` handling. |
| OV-00200 | Done | High | Low | `<Space>ai` doesn't seem to work with local models — Fixed: (a) `ai_cycle_profile()` now also updates `chat.opts.profile` so model selector in chat mode takes effect; (b) builtin.lua chat prompt now includes "Do NOT return raw JSON" to prevent models from emitting tool JSON as text. Root cause: chat.opts.profile was pinned at session open and `spawn_streaming_request` preferred it over `active_profile`. (ovim-core/src/editor/ai_integration.rs:62, ovim-core/src/lua/builtin.lua:48) |
| OV-00201 | Done | High | Low | Local models fail to use chat tools — Fixed: (a) Created `chat_messages_to_ollama_json()` using Ollama native format (`tool_name` instead of `tool_call_id`, object arguments instead of stringified); (b) Added content-based tool call detection in `parse_ollama_stream()` — when model emits tool JSON in `message.content` instead of structured `tool_calls`, parser detects and extracts it. Still requires manual testing with Ollama. (ovim-core/src/ai/provider.rs:556, ovim-core/src/ai/stream_parsers.rs:455) |
| OV-00202 | Done | MEDIUM | High | [MOTION] Most motion handlers in motions.rs pass char indices to cursor.set_col() instead of grapheme indices — cursor.col() is documented as a grapheme index, but word motions (w/b/e/W/B/E), find/till (f/F/t/T), first_non_blank (^), last_non_blank (g_), screen motions (H/M/L), sentence motions, and bracket matching (%) all compute char-based columns. Multi-codepoint characters (emoji, flags, ZWJ sequences) cause cursor to land at wrong positions. OV-00138 fixed the conversion helpers and high-priority callsites, but these motion handlers remain unconverted. (ovim-core/src/editor/motions.rs) |
| OV-00203 | Done | HIGH | Low | [PANIC] `&content[..77]` byte-slice in `format_context_window` panics on multi-byte UTF-8 — Fixed: use `char_indices()` to find the last valid char boundary at or before byte 77. (ovim/src/api/state.rs:451) |
| OV-00204 | Done | MEDIUM | Low | [CMD] `ZZ` silently discards save errors and quits anyway — Fixed: `ZZ` now checks save result; on error shows message in status bar and does NOT quit. (ovim-core/src/editor/input/normal/pending_commands.rs:450) |
| OV-00205 | Done | MEDIUM | Low | [MOTION] `T` (backward till) returns true with zero cursor movement when target char is immediately left of cursor — Fixed: `till_char_backward()` now checks `i + 1 < char_col` (actual movement required) before returning true, symmetric with the forward `t` fix (OV-00087). Returns false on no-movement to avoid poisoning `;`/`,` repeat. (ovim-core/src/editor/motions/char_find.rs:139) |
| OV-00206 | Done | HIGH | Medium | [RENDER] Simple wrap path splits lines by char count, not display width — Fixed: replaced `chars.chunks(text_width)` with a display-width-aware character loop that tracks `row_width` and wraps when a character would exceed `text_width`. CJK/emoji now wrap correctly in the simple (non-highlighted) path. (ovim/src/ui/renderer/buffer.rs) |
| OV-00207 | Done | MEDIUM | Low | [RENDER] Line padding in no-wrap highlighted path uses `chars().count()` not display width — Fixed: replaced `chars().count()` with `UnicodeWidthStr::width()` in all three padding paths (highlighted wrap, simple wrap chunk padding, simple no-wrap). (ovim/src/ui/renderer/buffer.rs) |
| OV-00208 | Done | MEDIUM | Low | [RENDER] Hover/completion popup X position ignores horizontal scroll offset — Fixed: both hover and completion popups now subtract h_offset in no-wrap mode and use cursor_to_visual_with_decorations for wrap-mode visual column. Also added inline decoration width to display_col calculation. (ovim/src/ui/renderer/overlays.rs) |
| OV-00209 | Done | MEDIUM | Medium | [RENDER] Non-focused split windows use the focused window's global wrap map — vertical splits with different widths get wrong soft-wrap break points in the non-focused pane, causing gutter/content row count mismatches and wrong cursor position after switching focus. (ovim/src/ui/renderer/core.rs:182-244) **Fixed (roadmap 19):** the wrap map is now per-`Window`. `Window` gained `wrap_map: Option<WrapMap>` + `wrap_decoration_generation`; `Editor::ensure_wrap_map_for_window(idx, width)` builds a given window's map at *its* content width; `render_window_tree`'s leaf handler calls it per pane and `render_buffer` reads that pane's map via `WindowRenderContext::wrap_map_window_index`. `Editor::wrap_map()` / `ensure_wrap_map()` resolve to the focused window when a `WindowManager` exists (so the cursor overlay, `gj`/`gk`, and mouse hit-testing all use the focused pane's map at the focused pane's width), else the editor-global `ViewportState::wrap_map` (headless / test-harness fallback). Tests: `per_window_wrap_tests` (ovim-core unit) + `ovim/tests/per_window_wrap_test.rs` (real split-layout render via `render_editor_to_ansi`, focus-switch-doesn't-reflow, single-window regression). See `refactor-roadmap/19-per-window-wrap-map.md`. Note: `Window::width`/`height` are still updated only via `WindowNode::update_dimensions` (not during `render_window_tree`), but `ensure_wrap_map_for_window` is fed the live `BufferLayout::compute(...).text_width` so the map width is correct regardless. |
| OV-00210 | Done | MEDIUM | Low | [LSP] `did_open()` TOCTOU race — Fixed: both `did_open` and `did_open_broadcast` now insert into `document_versions` inside the same lock guard as the `contains_key` check, so a duplicate caller short-circuits with `Ok(())`. Failure paths (missing server, notify error) roll back the claim so the next attempt isn't silently masked. Tests cover all three branches. (ovim-core/src/lsp/notifications.rs) |
| OV-00211 | Done | MEDIUM | Low | [LSP] No-diff early return in `send_did_change_with_version` bumps version without sending — when `compute_simple_diff` returns None, version is already incremented but `last_sent_versions` is never updated. In `flush_pending_changes_broadcast`, `any_sent` stays false so `last_sent` diverges permanently, causing unversioned diagnostics to be rejected. (ovim-core/src/lsp/notifications.rs:121-123,472-475) |
| OV-00212 | Done | MEDIUM | Low | [ERROR] `Change::ResourceOp` undo/redo silently ignores all filesystem errors — Fixed: `restore_file_snapshot` now returns `io::Result<()>`, `Change::apply/undo` propagate, `ChangeManager::undo/redo` return new `UndoOutcome { Nothing, Done, Failed(io::Error) }`, and `Editor::undo/redo` push a sticky System/Error toast on `Failed`. Failed changes are restored to their original stack so the user can retry once the FS condition is resolved. Coverage in `ovim/tests/resource_op_undo_errors_test.rs`. (ovim-core/src/change.rs:408-470, 749-816) |
| OV-00213 | Done | LOW | Low | [RENDER] Hover window content width uses `.len()` (bytes) instead of display width — Fixed: both styled preview lines and raw hover text now use terminal display width, with CJK regression coverage. (ovim/src/ui/renderer/overlays.rs) |
| OV-00214 | Done | LOW | Low | [RENDER] Tab bar and progress line width calculations use byte length — Fixed: tab widths were corrected under OV-00314; progress messages now also use terminal display width for right alignment, with CJK regression coverage. (ovim/src/ui/renderer/status_widgets.rs) |
| OV-00215 | Done | LOW | Low | [DEAD CODE] `lsp_command_rx` channel created in `enable_lsp()` but never polled — Fixed: removed the producerless `LspCommand` enum, the unbounded sender/receiver fields, the unused sender accessor, and channel allocation. LSP work continues through the active intent/slot paths. (ovim-core/src/editor/lsp_integration.rs, lsp_subsystem.rs) |
| OV-00216 | Done | HIGH | Medium | [SYNTAX] `create_ts_insert_edit` / `create_ts_delete_edit` pass char indices as `Point.column` — Fixed: both functions now compute absolute byte position via `char_to_byte(line_to_char(line)) + byte_col` instead of mixing char positions with byte offsets. Parameters renamed to `byte_col` with doc comments. (ovim-core/src/buffer/highlighting.rs) |
| OV-00217 | Done | HIGH | Low | [SYNTAX] `shift_highlights_for_insertion` shifts byte-offset ranges by `chars().count()` — Fixed: call sites in text_ops.rs already convert to byte offsets; parameter renamed to `byte_col` with doc comment clarifying byte semantics. (ovim-core/src/buffer/highlighting.rs) |
| OV-00218 | Done | HIGH | Low | [SYNTAX] `shift_highlights_for_deletion` col params are char indices but cache has byte offsets — Fixed: call sites in text_ops.rs already convert to byte offsets; parameters renamed to `start_byte_col`/`end_byte_col` with doc comments. `create_ts_delete_edit` also fixed with same byte arithmetic pattern. (ovim-core/src/buffer/highlighting.rs) |
| OV-00219 | Pending | MEDIUM | Medium | [PERF] `rope.to_string()` called repeatedly during highlighting operations — `apply_incremental_syntax_edit`, `rebuild_highlight_cache`, `rebuild_viewport_highlight_cache`, and `apply_background_syntax` each call `self.rope.to_string()`, allocating the entire buffer as a String. For a 1MB file this is 1MB allocation per edit cycle. Consider passing a rope-backed byte provider to tree-sitter instead. (ovim-core/src/buffer/highlighting.rs:391,676,707,187) |
| OV-00220 | Pending | MEDIUM | Low | [SYNTAX] Code block cache creates fresh `SyntaxHighlighter` per block on every rebuild — each markdown code block allocates a new Parser, Query, and does a full parse. A markdown file with 50 code blocks creates 50 parsers. Parsers could be cached per language. (ovim-core/src/syntax/code_blocks.rs:125-134) |
| OV-00221 | Done | MEDIUM | Low | [SYNTAX] Markdown code-block caches now explicitly sort blocks by `line_start` once per rebuild and use a shared binary-search helper for both highlight lookup and membership checks. Per-visible-line lookup is O(log n), with regression coverage for block starts, exclusive ends, and gaps. (ovim-core/src/syntax/code_blocks.rs) |
| OV-00222 | Pending | LOW | Low | [SYNTAX] `highlights_for_line_range` computes byte offsets for ALL lines even when querying a small range — `line_start_bytes` is built for the entire file, not just the viewport range. For a 50k-line file viewing lines 1000-1050, this allocates and iterates all 50k lines. (ovim-core/src/syntax/highlighter.rs:170-175) |
| OV-00223 | Pending | LOW | Low | [SYNTAX] `highlights_for_line` iterates source lines to find byte offset — uses `source.lines().enumerate()` up to `line_idx`, which is O(line_idx). The range and bulk methods use precomputed offsets; this per-line fallback doesn't. Only used in specific cases but could be O(n) per call. (ovim-core/src/syntax/highlighter.rs:254-299) |
| OV-00224 | Done | HIGH | Low | [LSP] Over-large message body bytes not drained from reader — Fixed: drain oversized message body in 64KB chunks before continuing the read loop. Prevents permanent framing desynchronization. (ovim-core/src/lsp/server.rs) |
| OV-00225 | Done | HIGH | Low | [LSP] `shutdown()` never sends the LSP `shutdown` request — Fixed: `request()` gate now allows `"shutdown"` method through during `ShuttingDown` state. All other methods still rejected per OV-00156. (ovim-core/src/lsp/server.rs:1174) |
| OV-00226 | Done | HIGH | Low | [LSP] `col_to_utf16` treats grapheme index as char index — `cursor.col()` is a grapheme index but the function calls `.take(safe_col)` on a `chars()` iterator. All outbound LSP request positions (hover, completion, goto-definition, code actions) are wrong on lines with multi-codepoint graphemes. Sister bug: `utf16_to_col` returns char count not grapheme count, so all inbound positions (goto results, workspace edits) also land at wrong grapheme. (ovim-core/src/editor/lsp_integration.rs:1234,1265) |
| OV-00227 | Done | HIGH | Medium | [LSP] `accept_completion` ignores `textEdit` and `additionalTextEdits` — Fixed: accept_completion now prefers textEdit range (with UTF-16→char conversion) over trigger_col fallback. additionalTextEdits (auto-imports) applied in reverse order with proper position conversion. Handles both Edit and InsertAndReplace variants. (ovim-core/src/editor/ui_features.rs) |
| OV-00228 | Done | HIGH | Low | [LSP] `workspace/configuration` returns `-32601 Method not found` — Fixed: added handler that responds with an array of Value::Null (one per requested item). rust-analyzer, pyright, and clangd now get a valid response during initialization. (ovim-core/src/lsp/notifications.rs) |
| OV-00229 | Done | HIGH | Medium | [LSP] `apply_workspace_edit` applies both `changes` AND `document_changes` — Fixed: changed to if/else-if structure so document_changes takes priority per LSP spec. changes is only used as fallback when document_changes is absent. (ovim-core/src/editor/lsp_modules/workspace_edits.rs) |
| OV-00230 | Done | HIGH | Medium | [LSP] `stop_server` leaks `document_versions`, `last_sent_versions`, and `last_local_edit` entries — Fixed: stop_server now clears all three maps so restarted servers receive fresh didOpen notifications. (ovim-core/src/lsp/mod.rs) |
| OV-00231 | Done | MEDIUM | Low | [LSP] `apply_lsp_edits_to_buffer_index` claimed `did_open_sent = true` for workspace-edit-loaded buffers without sending didOpen — Fixed: dropped the unconditional flag set so the planner routes through `DocumentSyncRequestAction::DidOpen` for never-opened files while still firing didChange for opened ones. `mark_modified()` preserved so the change isn't dropped. (ovim-core/src/editor/buffer_manager.rs) |
| OV-00232 | Done | MEDIUM | Low | [LSP] `window/showMessageRequest` returns `-32601` — Fixed: added handler that responds with Value::Null (no action selected) to unblock server UI flows. (ovim-core/src/lsp/notifications.rs) |
| OV-00233 | Done | MEDIUM | Low | [LSP] Progress tracking is now keyed by `(server_id, progress_token)`, preserving concurrent operations from the same server and clearing only the operation named by an `End` notification. Regression coverage mixes string and numeric tokens from one server. (ovim-core/src/lsp/notifications.rs, ovim-core/src/lsp/mod.rs) |
| OV-00234 | Done | MEDIUM | Low | [LSP] `:OrganizeImports` now selects code actions by the standard `source.organizeImports` kind (including hierarchical child kinds) instead of treating any workspace edit as an organize-imports action. Command-only server fallbacks remain supported. Regression coverage rejects unrelated quick fixes carrying edits. (ovim-core/src/editor/lsp_modules/actions.rs) |
| OV-00235 | Pending | MEDIUM | Low | [LSP] Non-current buffers modified by workspace edits never get `force_send` or diagnostics refresh — `apply_lsp_edits_to_buffer_index` only calls `invalidate_hover_cache()` and `request_diagnostics_refresh()` for the current buffer. Cross-file renames leave other modified buffers with stale diagnostics until the user manually switches to them. (ovim-core/src/editor/buffer_manager.rs:378-382) |
| OV-00236 | Done | LOW | Low | [LSP] `current_diagnostic()` and the diagnostic popup now share one nearest-to-cursor selector, so the status line and popup agree when a line has multiple diagnostics. Cursor grapheme columns and LSP UTF-16 offsets are normalized before comparison; regression coverage includes a ZWJ emoji prefix. (ovim-core/src/editor/lsp_modules/diagnostics.rs) |
| OV-00237 | Resolved | LOW | Low | [LSP] `replay_operation` for queued `DidChange` always uses `version: 1` — Resolved by deletion: the entire `PendingOperation` queue/replay infrastructure was vestigial scaffolding that never had producers (the `pending_operations` Vec was always empty). Removed in this commit, eliminating the unreachable `version: 1` site along with it. |
| OV-00238 | Done | HIGH | Low | [LSP] Inlay hints don't refresh on scroll — Already fixed: commit 8aab169 made hints file-scoped (lines 0..line_count) so scrolling never invalidates them. Test coverage at inlay_hints.rs:336-353 explicitly validates scroll stability. Issue was stale. |
| OV-00239 | Done | HIGH | Low | [CMD] `handle_substitute_command` used `rfind('s')` to locate the substitute command — finds the last 's' in the entire command string including pattern/replacement text. `:s/test/best/g` parsed range as `:%s/test/be` because it found the 's' in "best". Fixed: caller now pre-splits range from command part before dispatch. (ovim-core/src/editor/input/commands.rs) |
| OV-00240 | Done | MEDIUM | Low | [CMD] Empty substitute pattern `:%s//bar/` didn't reuse last search — Vim behavior is to reuse the `/` register pattern when the substitute pattern is empty. Fixed: reads `get_last_search()` register; shows E35 error if no prior search exists. Also stores non-empty patterns in the search register. (ovim-core/src/editor/input/commands.rs) |
| OV-00241 | Done | MEDIUM | Low | [CMD] `is_command_with_pattern` was overly broad — matched any command containing `/` anywhere (e.g., `:e path/to/file`), causing file paths to be treated as pattern commands. Fixed: now only matches `s/`, `g/`, `g!/`, `v/` after stripping range prefixes. (ovim-core/src/editor/input/commands.rs) |
| OV-00242 | Done | MEDIUM | Low | [CMD] `:g/.../s/.../` didn't convert Vim backreferences — the global command's inline substitute used raw replacement text without converting `\1`→`$1`. Fixed: replacement now goes through `convert_vim_backrefs()`. (ovim-core/src/editor/input/commands.rs) |
| OV-00243 | Done | HIGH | Low | [API] `handle_edit_line` mixed byte offsets and char positions — `find()` returned byte offsets that were piped into `CharCol(...)` and `GraphemeCol(...)`, silently corrupting content on lines with non-ASCII chars before the match. Fixed: extracted `find_char_positions` helper that returns char offsets; pre-computed grapheme prefix length on the pre-edit line so `cursor_after` is real grapheme-space. Regression coverage: byte/char unit tests, replace-after-non-ASCII-prefix, undo round-trip on a `café déjà vu` line, redo-with-emoji-prefix. (ovim/src/event_loop.rs) |
| OV-00244 | Done | MEDIUM | Low | [CMD] Multi-line substitute targets wrong lines after the first replacement — Fixed: both substitute loops (`:s` and `:g//s//`) now iterate bottom-up so a line split can't shift yet-unprocessed lines, `\r`/`\t` replacement escapes are supported (Vim semantics; `\r` is how a multi-line replacement is actually produced), and the interactive `/gc` path remaps later match offsets across the inserted lines. Proven failing pre-fix; coverage in `substitute_multiline_test.rs` (10 tests incl. confirm/skip orderings). Original report: — when `new_text` contains a newline, `insert_text_at(line_idx, ZERO, new_text)` splits the line, pushing all subsequent lines down. The next loop iteration uses `line_idx + 1` which now refers to the just-inserted continuation line, not the originally-targeted line. Original lines past the first substitution are silently skipped. (ovim-core/src/editor/input/commands.rs:506-526) |
| OV-00245 | Done | LOW | Low | [UNICODE] `markdown_conceal::LineTransform::identity` was char-indexed but `apply_conceal` was byte-indexed — Fixed: both paths now maintain the documented `src.len() + 1` byte-offset map, and every byte of a multibyte scalar maps to the same view character through one shared helper. Regression coverage exercises identity and concealed multibyte prefixes. (ovim-core/src/markdown_conceal.rs) |
| OV-00246 | Done | LOW | Low | [UNICODE] Visual mode resume now clamps against grapheme count, not char count. Fixed: `restore_last_visual_selection` uses `crate::unicode::grapheme_count` for both `start_line_len` and `end_line_len`. The lenient clamp let `gv` land the cursor past the last grapheme on lines with multi-codepoint graphemes (emoji/ZWJ/flags). Coverage: 3 unit tests in `visual_mode::tests` exercising single-grapheme, mixed-content, and ASCII baseline cases. (ovim-core/src/editor/visual_mode.rs:40,46,54,60) |
| OV-00247 | Done | LOW | Low | [STYLE] `ai_workflow.rs` `let _ = self.reload_workflows()?;` discarded a unit — refactor leftover. **Fixed:** plain `?` call. |

| OV-00248 | Pending | LOW | Low | [CODE QUALITY] ~176 clippy warnings on workspace — includes `if let Err(_)` instead of `is_err()` (lsp/server.rs:488), `unwrap()` after `is_some()` (ai/provider.rs:38), unused `extracted` variable (ai/provider.rs:88), manual `checked_div` (commands.rs:1098), redundant closures, derivable Default impls, collapsible matches in filetree/insert/motions handlers, items-after-test-module, oversized arg counts. Mostly style, but the unused `extracted` may indicate dropped retry partial data. Run `cargo clippy --all-targets --fix` for mechanical ones. |
| OV-00249 | Done | LOW | Low | [SAFETY] Concealed-link extraction subtracted one from `src_to_view.len()` unconditionally — Fixed: lookup now uses `get`, then `last`, then a zero fallback, so even a malformed empty transform cannot underflow or index out of bounds. Regression coverage passes an empty map explicitly. (ovim-core/src/markdown_conceal.rs) |
| OV-00250 | Done | HIGH | Medium | [LINE-ENDINGS] Bracketed paste did not normalize `\r` — pasting CRLF from Windows clipboards / browser scrollback / Mac-classic sources left literal `\r` in the rope, rendering as `^M`. Fixed: added `buffer::normalize_for_buffer(text) -> Cow<str>` (ovim-core/src/buffer/line_ending.rs) which collapses both `\r\n` and bare `\r` to `\n`, then called it once at the top of `handle_paste_event` so all mode branches share the normalization. Cow path means LF-only paste is zero-cost. Coverage: 8 unit tests on the helper + 5 integration tests in `paste_line_endings_test.rs`. (ovim-core/src/editor/mod.rs:1822) |
| OV-00251 | Done | HIGH | Low | [LINE-ENDINGS] LSP-provided `new_text` is now CRLF-normalized at all four insert seams: `apply_lsp_edits` (workspace_edits.rs:51), `accept_completion_item` main edit (ui_features.rs:126), additional edits loop (ui_features.rs:173), `apply_lsp_edits_to_buffer_index` (buffer_manager.rs:361). All four route through the shared `buffer::normalize_for_buffer` helper introduced for OV-00250 — same primitive, different entry point. The completion path normalizes early so the cursor-positioning `split('\n')` logic also sees LF-only content. The `:s` non-confirm path was scoped out (uses `convert_vim_backrefs` for replacement-text semantics; separate concern from server-supplied CRLF). Coverage: 3 integration tests in `lsp_crlf_normalization_test.rs`. |
| OV-00252 | Done | MEDIUM | Medium | [LINE-ENDINGS] Mac-classic (bare `\r`) line endings undetected — Fixed (commit 98d40f4): `LineEnding::detect` now distinguishes LF/CRLF/CR/Mixed; bare-CR files normalize to LF in the rope on load and convert back to CR on save. Coverage in `line_endings_load_save_test.rs`. Original report: `LineEnding::detect` only scans for `\r\n` and otherwise returns `Lf`. ropey 1.6 treats bare `\r` as a line break, so a CR-only file is loaded with N logical lines but each line content includes a trailing `\r`. The 194 sites that call `trim_end_matches('\n')` (vs. only 1 in the codebase that strips `\r`) leave that `\r` in the rendered text → every line shows `^M` at end. On save the file is written as `Lf` (no conversion), preserving the `\r` chars but losing the line-ending identity. Either add a `Cr` variant + normalization on load, or unconditionally strip bare `\r` on load and warn. (ovim-core/src/buffer/line_ending.rs:11-22, ovim-core/src/buffer/file_io.rs:96-100) |
| OV-00253 | Done | MEDIUM | Low | [LINE-ENDINGS] Output-side bug class closed by roadmap 18 — added `Buffer::line_text(idx) -> Option<Cow<'_, str>>` (strips `\r?\n$` once) and `Buffer::line_grapheme_count(idx)` accessors, then deleted the old `Buffer::line()` to compiler-enforce the migration. All 228 callsites now route through the canonical accessor. The bug class (any `\r` in the rope reaching motions / search / render / yank / paste paths) is structurally closed regardless of how the `\r` got in. 195 cosmetic `\.trim_end_matches('\n')` calls remain on already-stripped Cow values — they're no-ops and can be swept in a follow-up. (refactor-roadmap/18-line-text-accessor.md) |
| OV-00254 | Done | MEDIUM | Low | [LINE-ENDINGS] Mixed line endings collapse to whichever appears first — Fixed (commit 98d40f4): `Mixed` is now a detected variant; all CR forms normalize to LF on load and a mixed file saves as LF-only (its per-line mixture can't be represented once normalized), eliminating the `\r\r\n` round-trip corruption. Coverage in `line_endings_load_save_test.rs`. Original report: `LineEnding::detect` returns `Crlf` if any `\r\n` exists; `replace("\r\n", "\n")` then normalizes only those, leaving any bare `\r` in the file as literal characters in the rope. On save, every `\n` is rewritten as `\r\n`, so the originally-bare `\r` chars become `\r\r\n` round-trips. Vim shows a `[mixed]` warning for this case; ovim silently corrupts. (ovim-core/src/buffer/line_ending.rs:14-22, ovim-core/src/buffer/file_io.rs:96-99,194-199) |
| OV-00255 | Done | MEDIUM | Low | [PICKER] Fuzzy finder doesn't wrap selection — `move_up()` clamps at 0 and `move_down()` clamps at `count-1` (no wraparound). User reports pressing Up at the top should jump to bottom. The inline completion popup (`completion.rs:79,86-89`) already wraps via `% items.len()`, so the editor is internally inconsistent. Files: `ovim-core/src/editor/picker/mod.rs:261-286`. **Fixed:** `Picker::move_down`/`move_up` now wrap (mirroring `CompletionMenu::select_next`/`select_previous`); page-wise `move_down_n`/`move_up_n` continue to clamp because half-page jumps shouldn't wrap. Tests in `picker_search_test.rs` updated to assert wrap behaviour and a new test pins the clamp-on-page-motion contract. |
| OV-00256 | Triage | UNKNOWN | Low | [LSP/TS] `grr` (find-references) doesn't fire on a React component when the same position works in nvim. Keybinding mechanics look intact (`gr` arms pending command in `pending_commands.rs:109-112`, then `R` calls `request_find_references` at `pending_commands.rs:54,347`). Likely a TS-LSP-side issue (typescript-language-server has known caveats around JSX identifiers / unsynced documents). User flagged as `[unsure]`. Reproduce inside a `.tsx` component and capture the LSP request/response with `:LspLog` or `--headless 2>&1 \| grep references`. |
| OV-00257 | Done | HIGH | Medium | [WRAP/INLAY] Cursor lands one visual row below where it should when an inlay hint at end-of-line position causes a soft wrap that wouldn't happen without the hint; cursorline highlight stays on the row above the cursor. Root cause: `compute_wrap_points_with_decorations` and `cursor_to_visual_with_decorations` iterate `line_text.chars().enumerate()` and only process decorations at `inline_widths[dec_idx].0 == _char_idx`. Decorations placed at `char_idx == line.chars().count()` (end-of-line position — common for type hints right after a trailing identifier) are never processed by the inner walker, so `WrapMap` undercounts visual rows. The renderer's `apply_inline_decorations` path *does* render the hint via the trailing `if !found { line.spans.push(...) }` branch, then `split_line_into_rows` wraps. Renderer says "2 rows", WrapMap says "1 row" → cursor placement disagrees with cursorline bg. Fix candidates: (a) drain remaining decorations in a post-loop pass that mirrors the renderer's append-after-content semantics, (b) clamp end-of-line decoration `char_idx` to `chars().count() - 1` and rely on row_col >= max_width fallthrough. (ovim-core/src/wrap.rs:74-126, ovim-core/src/editor/wrap_map.rs:244-321) **Fixed:** went with (a) — added a post-loop drain in both `compute_wrap_points_with_decorations` and `cursor_to_visual_with_decorations` that walks any decoration whose char_idx is at-or-beyond the line's last character, mirroring the renderer's append-after-content fallthrough. The drain tracks `remaining` columns ahead so an exact-fill at the very last column does not push a spurious wrap point — that boundary is where the walker and renderer most easily disagree. Independently, fixed `make_line_text` in `ensure_wrap_map` to strip the trailing line terminator so the wrap walker walks the same characters the renderer renders (the previous code had `let trimmed = text` which was a dead-code rename, leaving `\n` in the walker's view but stripped on the renderer side). New tests in `inlay_hint_wrap_cursor_test.rs` pin the WrapMap-vs-renderer row-count contract for end-of-line decorations across pushes-wrap, within-width, exactly-at-width, and multi-row-drain cases. |
| OV-00258 | Done | HIGH | Medium | [INLAY] Type hints render at the wrong place — confirmed by user screenshot showing hints sliced into the middle of identifiers (`r:string\|undefinedawEmbedParam`, `clearEmbedCook:string\|nullie`) and even injected into a comment line that has no hints (`s:boolean` inside `// ...header to the same-request SSR`). Root cause in `poll_pending_inlay_hint_response` (ovim-core/src/editor/lsp_integration.rs:970-986): when a stale hint reply lands (LSP `result.request_key.lsp_version < current buffer version` because the user kept typing), the code (a) interprets the LSP `Position {line, character}` against the *current* rope's `line_to_char(line) + char_idx` mapping — so positions from the old buffer state collide with completely different content in the new buffer, and (b) tags every produced decoration with `source_version = self.buffer().version()` (current). Since `edits_since(current)` is empty, `project_decoration` is a no-op forever — the projection machinery exists but never gets a chance to replay the edits between the LSP version and current. Net effect: stale hints are pinned at coordinates that meant something at lsp_version and now point into mid-identifier / mid-comment positions in the post-edit buffer. The `if result.buffer_version != current { invalidate_debounce }` at line 997 *requests* a refresh on the next tick but doesn't suppress the stale render. Fix candidates: (1) drop hints when `result.buffer_version != current` rather than rendering them mis-aligned; (2) tag `source_version = result.buffer_version` and convert the LSP positions against a rope snapshot at that version, then let `project_decoration` replay edits forward — requires either keeping rope snapshots at LSP request boundaries or replaying the edit log backward to derive historical line→char mappings; (3) project the LSP positions through `edit_log.edits_since(result.buffer_version)` *before* converting to char offsets. Independent of the projection bug, two pre-existing concerns remain: `display_width = text.chars().count()` in `decorations_from_inlay_hints` (decoration.rs:530) is char-count not display-width (wide-char hint labels mis-size their slot), and `inline_width_before_projected` uses `idx <= char_idx` (decoration.rs:422) which mixes "before-or-at-cursor" semantics with the renderer's "decoration inserted between spans" placement — both worth tightening once the projection bug is closed. **Fixed:** option (1) — `poll_pending_inlay_hint_response` now drops the reply when `result.buffer_version != self.buffer().version()` (before any decoration placement) and invalidates the slot so the next tick re-requests against the current buffer. Options (2) and (3) (project LSP positions through the edit log) are documented in-source as future work; not justified while OV-00259 keeps inlay hints off-by-default. The two pre-existing concerns (`display_width = chars().count()` and `inline_width_before_projected` `<=` semantics) remain open for a follow-up. |
| OV-00259 | Done | HIGH | Low | [INLAY] Disable inlay hints by default while the wrap/placement bugs are open. Currently `EditorOptions` (ovim-core/src/editor/mod.rs:149-206) has *no* inlay-hints field, and `process_lsp_sync_and_inlay_hints` (ovim/src/event_loop.rs:144-146) unconditionally requests hints whenever the LSP supports the capability — there is no off-switch and no `:set` toggle. Required: (a) add `pub inlay_hints: bool` to `EditorOptions`, default `false`; (b) gate `request_inlay_hints_refresh` and `inlay_hints_refresh_needed` behind it; (c) clear `lsp.state.inlay_hints` and `decorations.replace_source(InlayHint, [])` on toggle-off so stale hints disappear immediately; (d) wire up `:set inlay_hints` / `:set noinlay_hints` in `cmd_set.rs`. Relates to OV-00257, OV-00258. **Fixed:** added `EditorOptions.inlay_hints` (default `false`); both `inlay_hints_refresh_needed` and `request_inlay_hints_refresh` early-return when off; new `clear_inlay_hints_state` wipes cached hints + `InlayHint` decorations on toggle-off, `invalidate_inlay_hints_slot` re-arms the next-tick refetch on toggle-on; `:set inlay_hints` / `:set noinlay_hints` / `:set inlay_hints?` wired in `cmd_set.rs`. |
| OV-00260 | Done | HIGH | Low | [INLAY/CURSOR] `inline_width_before_projected` over-counts EOL hints when cursor is at end-of-line in insert mode. The `<=` comparison (decoration.rs:295,422) means a hint anchored at `char_idx == line.chars().count()` (typical EOL type hint position) is counted as "before the cursor" when cursor is at the same position. Insert-mode `A` puts the cursor at `chars().count()` — visually that should be between the buffer end and the hint, but the code adds the hint width to `display_col`, so the cursor renders *past* the hint instead of in the gap. Normal-mode `$` is unaffected because cursor lands at `chars().count() - 1`. The right semantic: include hint at `idx` when `idx < char_idx` always, AND when `idx == char_idx` only if there's a buffer character at that position (`idx < line_chars_count`). Test gap: `hint_at_end_of_line_does_not_affect_dollar` covers `$` only — needs an `A`-mode counterpart. (ovim-core/src/editor/decoration.rs:288-304, 409-431) **Fixed:** both `inline_width_before` and `inline_width_before_projected` now count a decoration at `idx` when `idx < char_idx`, or `idx == char_idx && idx < line_content_len`. Added a `line_content_char_len(rope, line)` helper (strips a single trailing `\n` / `\r\n` / `\r`) for the "is there a buffer character here?" boundary — since folded into `display::line_content_len` by the line-content-helpers consolidation (`f1839e9`). Unit coverage: updated `inline_width_before` test (the EOL-hint case now correctly returns 5, not the old buggy 13) and a new `eol_hint_not_counted_for_cursor_at_eol` pinning both the `$` (char 9) and `A` (char 10) positions for both the raw and projected accessors. (A renderer-level `A`-mode integration test wasn't added — the test harness doesn't expose rendered cursor X / `h_offset` without a real render pass; the unit tests exercise the buggy function directly.) |
| OV-00261 | Done | MEDIUM | Low | [INLAY/UNICODE] `decorations_from_inlay_hints` and `decorations_from_diagnostics` size their `display_width` field with `text.chars().count()` instead of `display::display_width(text, tab_width)`. For ASCII labels (`": i32"`) the values agree, but a hint label containing wide chars (Japanese type names like `": 文字列"`, fullwidth colon `"：i32"`, or any CJK) under-sizes the slot — wrap math undercounts visual rows and the cursor lands a row above the hint. The fix is mechanical: replace `text.chars().count()` with `crate::display::display_width(&text, /*tab_width=*/ 1)` (decorations don't contain tabs). Same bug in two adjacent functions. (ovim-core/src/editor/decoration.rs:530, 619) **Fixed:** `decorations_from_inlay_hints` and `decorations_from_diagnostics` both compute `display_width` via `crate::display::display_width(&text, 1)` now. Status: marked Done — change is mechanical and exercised by the existing decoration tests; the wide-char wrap-undercount path is hard to unit-test without CJK fixtures, left to the broader Unicode-fixture follow-up. |
| OV-00262 | Done | MEDIUM | Low | [LSP/CAPS] Client capabilities don't advertise `text_document.inlay_hint` so spec-compliant servers may skip sending hints. `ClientCapabilities::text_document` (lsp/server.rs:735-792) declares completion, hover, signature_help, references, code_action, rename, formatting, publish_diagnostics — but NOT `inlay_hint`. LSP 3.17 servers that gate on `client.capabilities.textDocument.inlayHint` (the same pattern that bit OV-00054 for diagnostics) won't emit hints. We then ask via `textDocument/inlayHint`, the server returns `null` or an error, and we silently get an empty Vec. With OV-00259 turning hints off-by-default the user impact is masked, but the moment hints are flipped on for a server that obeys the capability handshake, they'll be empty for no obvious reason. Fix: add `inlay_hint: Some(InlayHintClientCapabilities { dynamic_registration: Some(true), resolve_support: None })`. (ovim-core/src/lsp/server.rs:735-792) **Fixed:** added `inlay_hint: Some(InlayHintClientCapabilities { dynamic_registration: Some(true), resolve_support: None })` to `TextDocumentClientCapabilities` in `do_initialize`. |
| OV-00263 | Done | LOW | Low | [WRAP] `WrapMap::invalidate_line` is not decoration-aware. It calls `crate::wrap::visual_line_count` (the no-decorations form) instead of `visual_line_count_with_decorations`, so any line with inlay hints would be miscounted if invalidated incrementally. Currently only used in tests (only callsite is `wrap_map.rs:544` inside `#[test]`), so production routes through `rebuild_with_decorations` and stays correct — but the foot-gun lives unbeholden. Either make `invalidate_line` take an `inline_widths` callback to match `rebuild_with_decorations`, or delete it as unused. Related to OV-00015 (incremental invalidation). (ovim-core/src/editor/wrap_map.rs:147-167) **Fixed:** deleted `WrapMap::invalidate_line` and its test outright — zero production callers (the renderer always full-rebuilds via `ensure_wrap_map` → `rebuild_with_decorations`), it ignored inline decoration widths, and its `isize` offset arithmetic could underflow (subsumes **OV-00191** too). Left a comment in `wrap_map.rs` noting that incremental invalidation (OV-00015) should be (re)built decoration-aware from scratch when wired up. |
| OV-00264 | Done | MEDIUM | Low | [LINE-ENDINGS] ~31 callsites bypassed `Buffer::line_text()` / `display::line_content()` and used raw `rope.line(idx).to_string()` — and post-`9c4d92c` several stripped *nothing* (the sweep had replaced `.trim_end_matches('\n')` with a no-op `let x = x;` rebinding at the `rope.line()`-sourced sites, so `chars` carried the terminator). Canonical accessor is `Buffer::line_text(idx) -> Option<Cow<'_, str>>` (for `&Buffer` holders) / `display::line_content(rope, idx)` (for `&Rope` holders), both stripping `\r\n`/`\n`/`\r` uniformly. **Fixed:** routed every site through `display::line_content` / `display::line_content_len`: `motions/char_find.rs` (`f`/`t`/`F`/`T`), `motions/word.rs` (`w`/`b`/`e`/`ge`, incl. `find_next_word_start` / `word_end_forward_once` / `word_end_backward_once` — restoring the pre-`9c4d92c` content semantics), `motions/bracket.rs` (`%` + `[{`/`]}`/`[(`/`])` — also killed `.to_string().to_string()` and **fixed a real bug**: the enclosing-brace motions built `abs_pos` by summing `buffer.line_text(i).chars().count()` (terminator-*excluded*) while indexing into `rope.to_string()` (terminator-*included*), so `[{`/`]}`/`[(`/`])` from any line > 0 searched from a position `line_idx` chars too early — now `rope.line_to_char(line_idx) + grapheme_to_char_col(line_content, col)`), `textobjects.rs` (`iw`/`aw`/quote & bracket objects + the "last line of file" `end_col`, latent off-by-one when the last rope line wasn't the phantom), `ui/renderer/overlays.rs` (hover/completion popup positioning ×2), `ui/renderer/buffer.rs` (main paragraph path), `ui/renderer/core.rs` (cursor positioning), `event_loop.rs` (`handle_edit_line` find + `read-lines` API, which was returning each line *with* its `\n` — `// Strip trailing newline` over a no-op `.to_string()`), `editor/decoration.rs` (test fixture). `editor/lsp_integration.rs`'s UTF-16 helpers + the inlay-hint closure (OV-00265) were already terminator-aware. Removed ~12 `let x = x;` / `to_string().to_string()` artifacts; `clippy::redundant_locals` clean. ovim-core 748 lib tests + full `cargo test --workspace` green. |
| OV-00265 | Done | LOW | Low | [INLAY] `decorations_from_inlay_hints`'s line_text closure (lsp_integration.rs:1010-1016) does `rope.line(line_idx).to_string().to_string()` — double `.to_string()`. The first call (on `RopeSlice`) already produces a `String`; the second is a no-op clone. Same anti-pattern in `editor/decoration.rs:930` and `editor/motions/word.rs:480`. Independently, the line_text passed to `decorations_from_inlay_hints` includes the trailing newline, but `utf16_to_char_col` skips it for in-bounds positions. If an LSP server returns `position.character > line_length_utf16` (rare but seen in some servers), the newline gets counted as a char and the decoration is anchored on the next line. Latent. (ovim-core/src/editor/lsp_integration.rs:1010-1016, ovim-core/src/editor/decoration.rs:491-543) **Fixed:** the production closure in `poll_pending_inlay_hint_response` now routes through `Buffer::line_text` (single allocation, strips `\r\n` / `\n` / `\r`), which also closes the latent past-end-of-line newline-counting anchor bug. Killed the double `.to_string()` in the `decoration.rs` test fixture and in `motions/word.rs` (the broader raw-`rope.line()` sweep stays OV-00264). |
| OV-00266 | Done | LOW | Low | [CMD] `:set foo!` (toggle syntax) not implemented. Vim treats `:set foo!` as "flip foo" — `cmd_set.rs:425-444` parses `=` separators and `?` queries but never tests for a trailing `!`. Bool options (`number`, `wrap`, `expandtab`, `inlay_hints`, etc.) all need separate `:set foo` / `:set nofoo` invocations. Trivial fix: in `handle_set_command`, after the `?` strip, also handle `strip_suffix('!')` by reading current value and dispatching to the opposite of `try_set_bool`. (ovim-core/src/cmd_set.rs:400-454) **Fixed:** `handle_set_command` strips a trailing `!` (right after the `?` query check) and calls a new `try_toggle_bool`, which flips the value via the `BOOL_OPTIONS` get/set pair — and for `inlay_hints` routes through `try_set_bool("inlay_hints"/"noinlay_hints")` so the cache-clear / slot-invalidate side effects still fire. `:set foo!=bar` and non-boolean `foo!` are rejected. Tests cover `number!`, alias `wrap!`, `inlay_hints!` side effects, and the error cases. |
| OV-00267 | Done | LOW | Low | [LSP/COMPLETION] `text_edit_majority_start` uses `HashMap` for tally, then `max_by_key`. For ties, `max_by_key` keeps the first-seen max, but HashMap iteration order is randomized per process — so two completion items with equally popular `textEdit.range.start.character` produce a non-deterministic trigger column across runs. Visible in multi-server completion (e.g., Java + JDTLS extensions both contributing) where the prefix length flickers between identical-popularity choices. Fix: use `BTreeMap<u32, usize>` for stable iteration, or a `Vec<(u32, usize)>` with `.iter().max_by_key()` falling back to the lower start position. (ovim-core/src/editor/lsp_modules/completion.rs:277-294) **Fixed:** tally is a `BTreeMap<u32, usize>` now and the winner is picked with `max_by(|a, b| count_a.cmp(count_b).then_with(|| start_b.cmp(start_a)))` — total order, no iteration-order dependence; on a popularity tie the lower start column (longer replacement) wins. New test `majority_start_tie_prefers_lower_column_deterministically` checks the answer is stable under input reordering. |
| OV-00268 | Done | MEDIUM | Low | [LSP/UNICODE] `col_to_utf16` strips `\n` from the line but not `\r`. If a buffer somehow contains a bare `\r` (mixed line endings per OV-00254, or a future line-ending bug), the LSP `Position.character` will count the `\r` toward UTF-16 offsets — but the LSP server, which never saw the `\r` (we strip on send via `compute_simple_diff`), interprets the position against its own line content. Result: hover, goto-def, completion, find-references all misalign by one UTF-16 unit per stray `\r` left of the cursor. Fix: filter `c != '\n' && c != '\r'` in `take_while`, mirroring `Buffer::line_text` semantics. Also applies symmetrically to `utf16_to_col` if it has the same shape. (ovim-core/src/editor/lsp_integration.rs:1888-1912) **Fixed:** `col_to_utf16` and `utf16_to_grapheme_col` now `take_while(|&c| c != '\n' && c != '\r')`; `utf16_to_col` breaks the loop on `\n`/`\r` so a server-supplied past-EOL offset can't advance into the terminator. |
| OV-00269 | Done | LOW | Low | [LSP/STATE] `clear_lsp_state` clears `lsp.state.inlay_hints` but not `lsp.state.current_file_diagnostics`. The cached diagnostic vector lingers until the next refresh fires (which then overwrites it). Decorations are wiped via `decorations.clear()` at the bottom, so the user-visible state stays consistent — this is purely a "stale data lives in the struct" issue. Symptom-free in practice but inconsistent with how `inlay_hints` is treated. Either clear both or document the asymmetry. (ovim-core/src/editor/lsp_integration.rs:1080-1097) **Fixed:** `clear_lsp_state` now also `self.lsp.state.current_file_diagnostics.clear()`, alongside the existing `inlay_hints.clear()`. |
| OV-00270 | Done | LOW | Low | [DIAG] `diagnostic_counts` (lsp_modules/diagnostics.rs:9-25) treats `severity: None` as WARNING. LSP 3.17 §`Diagnostic.severity` says clients SHOULD interpret missing severity as "implementation-defined", but VS Code defaults to ERROR. Treating None-severity items as warnings under-reports ERROR counts in the status line for servers that omit severity (some Java/Scala tooling does). Fix: change `None => warnings += 1` to `None => errors += 1` to match VS Code, OR document the choice explicitly. (ovim-core/src/editor/lsp_modules/diagnostics.rs:20) **Fixed:** `None => errors += 1` now (commented with the VS Code rationale). New tests `diagnostic_counts_by_severity` / `diagnostic_counts_missing_severity_is_error`. **Follow-up (Goldilocks):** also unified the *display* path — `diagnostic_style`, `severity_ord`, and the EOL-decoration `priority` in `decorations_from_diagnostics` all treat missing/unknown severity as ERROR now (was: HINT styling / lowest rank). New test `diagnostics_missing_severity_treated_as_error`. |
| OV-00272 | Done | HIGH | Low | [WRAP/CURSOR] Hardware cursor misplaced on wrapped lines containing wide chars (CJK/emoji), reported as "cursor on the wrapped last char draws over a char earlier in the line". `render_cursor` (ovim/src/ui/renderer/core.rs) computed the cursor's "flat display column" from `expand_tabs_with_mapping`'s `char_mapping` — an expanded CHAR INDEX — but `WrapMap::cursor_to_visual_with_decorations` expects a display COLUMN (sum of char display widths). Tabs and control chars expand to width-1 chars so the two coincide; wide chars are 1 char / 2 columns, so the computed column came up short. Depending on parity the walk either matched a wrong earlier cell (cursor a full visual row too high, drawn over an unrelated mid-row character) or matched nothing and fell through to end-of-line. **Fixed:** `char_col_to_display_col` (ovim-core display helper: flat tab stops + `char_display_width`) replaces the char-index computation; also corrects the nowrap horizontal-offset path. Regression tests `cursor_after_wide_chars_uses_display_columns`, `cursor_on_wrapped_wide_char_lands_on_wrapped_row`, `cursor_on_wrapped_last_char_sits_on_second_visual_row` (full render → hardware cursor), all proven failing pre-fix. |
| OV-00273 | Done | HIGH | Medium | [WRAP] Wrap math disagreed with the renderer about tabs past the first wrap boundary. The renderer expands tabs FLAT against the raw line, then splits into rows (a tab's spaces can straddle a row break); `compute_wrap_points`/`cursor_to_visual` computed tab widths from the ROW-RELATIVE column (reset at each wrap) and wrapped tabs atomically. Whenever `text_width % tab_width != 0` and a line wraps with tabs after the boundary, row counts/positions diverged — a sweep found 3231 mismatching (width, tab_width, content) cases — shifting every visual row below the line: cursor drawn on the wrong row, mouse clicks resolving the wrong position, scroll math off. `cursor_subline_in_wrapped_line` (editor/mod.rs) already assumed flat stops, disagreeing with the walkers it consumed. **Fixed:** `compute_wrap_points_with_decorations`, `WrapMap::cursor_to_visual_with_decorations`, and `WrapMap::sub_line_display_range` now use a flat content-only column as the tab-stop base and consume tabs column-by-column (lazy wrap before each cell), matching the renderer exactly; tab-stop base also excludes decoration widths, since the renderer expands tabs before splicing inlay-hint spans. Consistency fuzz `wrap_count_matches_flat_tab_expansion` (ovim-core/src/wrap.rs) pins map == flat-expansion row counts. Full-render regression `tabs_past_wrap_boundary_keep_cursor_and_content_aligned` (core.rs). |
| OV-00274 | Done | MEDIUM | Low | [WRAP/DECORATIONS] Decoration column accounting in `compute_wrap_points_with_decorations` silently dropped one cell whenever a decoration (inlay hint) began on an exactly-full row: `current_width += 1` overflowed past `max_width`, then the `>= max_width` check reset to 0 — losing the cell that should have started the next row. Same defect in the end-of-line drain (its `remaining` guard also let the first overflow cell escape). Result: `WrapMap` undercounts visual rows vs. the rendered line (231 mismatching cases in a sweep of single-decoration layouts), so the cursor/scroll drift by a row below lines whose hints land on row boundaries. **Fixed:** both cell loops wrap lazily BEFORE consuming each cell (mirroring the tab-cell walk); the drain keeps exact-fill-at-end emitting no spurious row (OV-00257 behavior preserved). Fuzz `decoration_row_count_matches_composed_text` (ovim-core/src/wrap.rs) pins map == composed-text row counts. |
| OV-00275 | Done | LOW | Low | [WRAP] Residual: `cursor_subline_in_wrapped_line` (ovim-core/src/editor/mod.rs:1517) re-walked wrap points at raw char granularity, so a tab split across a row boundary (possible since OV-00273) was attributed entirely to the row that starts at the tab's char index — scrolloff math off by one sub-line when the cursor sat inside a split tab. **Fixed:** extracted the column-level walk from `WrapMap::cursor_to_visual_with_decorations` into `wrap::visual_position_for_flat_col` (ovim-core/src/wrap.rs); both the wrap map and `cursor_subline_in_wrapped_line` now delegate to it, so there is a single cursor-to-visual walker. Regression tests in `wrap::visual_position_tests` cover the split-tab attribution. |
| OV-00276 | Pending | LOW | Low | [RENDER] The simple (no-highlight) wrap path in `render_buffer` (ovim/src/ui/renderer/buffer.rs:~2188) splits rows with `row_width + ch_width > text_width && !row_text.is_empty()`, while the detailed path's `split_line_into_rows` (same file, ~1225) has no `!empty` guard — at pathological widths (text_width 1 with wide chars) the detailed path emits a leading blank row (agreeing with `compute_wrap_points`, which pushes a wrap point at char 0) and the simple path does not. The two render paths can disagree with each other and with the wrap map by one row. Only reachable with a 1-2 column text area; unify the guard (and decide whether a char wider than the row gets a blank row above it) when touching these paths. |
| OV-00277 | Done | HIGH | Medium | [LANG/CLI] `ovim lsp check` and `ovim lsp languages` consulted the static `LanguageRegistry` only, never loading init.lua — languages registered dynamically via `ovim.languages.register` (e.g. a user's custom language) were invisible in exactly the diagnostic commands used to verify a new language setup, reporting "No language configuration found" while the editor itself detected and served the language fine. **Fixed:** added `LanguageCatalog::all()` + `LanguageCatalog::with_user_languages()` (ovim-core/src/language_catalog.rs) backed by `lua::register_user_languages` (ovim-core/src/lua/mod.rs), which runs init.lua + plugins against a throwaway bridge; `cmd_check_lsp`/`cmd_list_languages` (ovim/src/subcommands.rs) now use the catalog and print the registering source for non-built-in languages. |
| OV-00278 | Done | HIGH | Low | [CLI/FILE-OPS] `atomic_write` (ovim/src/subcommands.rs:654-675) uses `path.with_extension("tmp")` — REPLACING the extension. `ovim edit foo.sh` writes to `foo.tmp`, silently destroying any pre-existing user file of that name, and the rename recreates the target with default perms (exec bit lost, 755→644), replaces symlinks with regular files, breaks hard links; concurrent edits of `a.rs`/`a.json` collide on `a.tmp`. Violates the invariant `Buffer::save_as_async` deliberately preserves. Affects `edit`, `insert`, `delete-lines` file mode. Use a unique temp name (append, not replace) + preserve metadata, or write in place like the buffer save path. **Fixed:** writes in place on the existing inode (like Buffer::save_as_async), preserving perms/links/symlinks and never touching sibling files; fn renamed overwrite_file. |
| OV-00279 | Done | HIGH | Low | [SESSION/API] Session-mode `insert --after <last>` splices into the last line when the buffer has no trailing newline — `handle_insert_lines` append branch (ovim/src/event_loop.rs:1992-1996) inserts `"TEXT\n"` at `len_chars`, producing `alpha\nbetaTEXT\n` instead of `alpha\nbeta\nTEXT`. File mode handles this correctly; reachable via CLI `-s`, `POST /v1/insert`, and MCP. Prepend `\n` when appending past a terminator-less last line. **Fixed:** append branch now leads with \n when the last line lacks a terminator and drops the trailing newline, matching file mode. |
| OV-00280 | Done | MEDIUM | Low | [CLI/FILE-OPS] Line-based file ops rewrite every CRLF to LF — `cmd_edit_file --line` (ovim/src/subcommands.rs:431,463-472), `cmd_insert_file` (:516,561-566), `cmd_delete_lines_file` (:575,601-606) read via `content.lines()` (strips `\r`) and re-join with `"\n"`, so a one-line change produces a whole-file line-ending diff. Whole-buffer `edit` (no `--line`) is unaffected, so `--old` matching semantics also differ between branches of the same command. **Fixed:** added line_separator(): all three ops re-join with \r\n when the file is CRLF. |
| OV-00281 | Done | MEDIUM | Low | [CLI] `ovim session list` panics on non-ASCII file paths — byte slicing `&f[f.len() - 37..]` (ovim/src/subcommands.rs:707) can split a char boundary; a session on a path with multibyte chars makes the whole listing command panic (exit 101) until that session dies. Use char-aware truncation. **Fixed:** char-aware truncation via chars().skip(). |
| OV-00282 | Done | MEDIUM | Low | [CLI/LSP] One-shot LSP commands on files whose language has no LSP (markdown, HCL) stall the full 30s then report a misleading "Timeout waiting for LSP to be ready" — `wait_for_lsp_ready` (ovim/src/subcommands.rs:110-113) requires a non-empty server list, but such files spawn zero servers. Treat an empty server list as ready and report "No LSP servers for this file type" immediately. **Fixed:** empty server list treated as ready after a 1.5s startup grace period. |
| OV-00283 | Done | MEDIUM | Low | [API] JSON endpoints return HTTP 200 for editor errors — `set_mode` (ovim/src/api/handlers.rs:289-292), `paste` (:106-109), `resize` (:140-143) serialize the error variant with status 200; `OvimClient::paste/resize/set_mode` (ovim/src/client.rs:78-109,140-154) check only HTTP status, so `ovim paste`/`ovim resize` and `cmd_hover`'s mode reset exit 0 on failure. Return 4xx for error variants or inspect bodies client-side. **Fixed:** added json_api_response(): all 20 REST handler sites now map ApiResponse::Error to HTTP 400. |
| OV-00284 | Done | MEDIUM | Low | [SESSION/API] `edit --line` ambiguity guard exists only in file mode — session mode (`handle_edit_line`, ovim/src/event_loop.rs:1917-1924) only rejects multiple matches when `--line` is NOT given... file mode refuses `--line` + multiple matches on that line ("found 2 times. Be more specific") while session mode silently replaces the first occurrence. Same CLI, opposite safety contract; agents relying on the documented uniqueness guard can corrupt the wrong occurrence. **Fixed:** session mode rejects multiple matches also when --line is given ("Be more specific"), matching file mode. |
| OV-00285 | Done | MEDIUM | Low | [SESSION/API] Session-mode `edit` can never match a multi-line `--old` — `handle_edit_line` (ovim/src/event_loop.rs:1885-1909) searches per-line terminator-stripped content, so the CLI-documented `\n` expansion in `--old` never matches; file mode matches multi-line text fine via whole-content `replacen`. Proven: buffer `alpha\nbeta`, `--old 'alpha\nbeta'` → "Text not found" in session mode only. **Fixed:** multi-line needles matched against the whole buffer with absolute char offsets; delete_range spans lines; multi-line --old + --line rejected explicitly. |
| OV-00286 | Done | HIGH | Low | [TEXTOBJ] `dit`/`dat` off-by-one: `content_end = search_pos.saturating_sub(1)` and `closing_tag_end = name_end` (ovim-core/src/textobjects.rs:634-635) treat ends as inclusive but `TextObjectRange.end_col` is exclusive downstream — `dit` on `x<b>abc</b>` leaves `x<b>c</b>` (last content char kept), `dat` leaves `x>` (closing `>` kept). **Fixed:** both ends now exclusive (content_end = search_pos, closing_tag_end = name_end + 1); empty check adjusted. |
| OV-00287 | Done | MEDIUM | Low | [TEXTOBJ] Tag text object never matches a tag whose `<` sits at buffer offset 0 — backward scan `while i > 0` (ovim-core/src/textobjects.rs:570-594) never tests index 0; `dit` inside `<b>abc</b>` at buffer start is a no-op. **Fixed:** backward scan rewritten as loop that tests index 0. |
| OV-00288 | Done | HIGH | Low | [MOTION] Backward find/till operators (`dF`/`dT`/`cF`/`cT`/`yF`/`yT`) wrongly include the cursor character — vim's backward F/T are exclusive. Two independent sites: interactive `OperatorRange::inclusive(end, start)` (ovim-core/src/editor/input/char_motion.rs:210-212) and dot-repeat/buffer path `(found_col, col + 1)` (ovim-core/src/buffer/text_ops.rs:702-711). Repro: `axbc` cursor on `c`, `dFx` → `a` (vim: `ac`). **Fixed:** interactive path uses OperatorRange::exclusive; buffer path drops the +1 on cursor col; four tests updated to vim-correct expectations. |
| OV-00289 | Done | MEDIUM | Low | [OPERATOR] `d{count}w` discards everything past the first line boundary — eol truncation in `delete_word_forward`/`_big` (ovim-core/src/buffer/text_ops.rs:807-816, 1248-1257) applies regardless of count; `d2w` on `one\ntwo three` leaves `\ntwo three` (vim: `three`). Truncation is only correct for single `dw` on the line's last word. **Fixed:** eol truncation now applies only to the final word motion (count-1 motions run first), matching vim's last-word-at-eol rule; dW same. |
| OV-00290 | Done | MEDIUM | Low | [OPERATOR] `{count}J` performs count joins instead of count-1 — `lines_to_join = count.max(1)` (ovim-core/src/buffer/text_ops.rs:362-364); `3J` on `a\nb\nc\nd` gives `a b c d` (vim: `a b c`). **Fixed:** lines_to_join = count.saturating_sub(1).max(1); repeat test updated. |
| OV-00291 | Done | MEDIUM | Low | [PASTE] Blockwise paste drops block rows past the last buffer line — `if target_line >= buf.line_count() { break; }` (ovim-core/src/editor/input/helpers.rs:702-704, 905-907); vim appends padded lines for remaining rows. Repro: block `a`/`c` pasted on last line loses the second row. **Fixed:** rows past the last line are appended as new space-padded lines; two visual-block tests updated. |
| OV-00292 | Done | MEDIUM | Low | [REGISTER] `y'a` (linewise yank to mark) glues lines: `apply_linewise_operator` yank concatenates `line_text()` without re-adding `\n` (ovim-core/src/editor/input/char_motion.rs:393-401); register for a 3-line yank contains one glued line. **Fixed:** yank loop re-adds \n per line. |
| OV-00293 | Done | MEDIUM | Medium | [OPERATOR] `d}`/`d{`/`y}`/`y{` misclassified the register as linewise and swallowed the eol newline — vim's exclusive col-0 adjustment was missing. **Fixed** via the new `MotionRange` model (OV-00300): paragraph operators now build `MotionRange::from_exclusive`, which applies vim's rules (exclusive end at col 0 retreats to the previous line's end; retreat from at/before first non-blank promotes to linewise). Mid-line d} keeps the blank separator and yields a charwise register; line-start d} is linewise. Regression tests in ovim/tests/hunt_regressions_test.rs. |

| OV-00294 | Done | LOW | Low | [REGISTER] Added a real small-delete (`"-`) register and route characterwise deletes without a line break there instead of rotating `"1`-`"9`; linewise/multiline deletes still rotate numbered history. Register selection now accepts `-`, listings expose it, and explicit named deletes update named + unnamed without touching implicit delete registers, matching Neovim 0.12.4. (ovim-core/src/editor/register.rs, ovim-core/src/editor/mod.rs, ovim-core/src/editor/input/normal/pending_commands.rs) |
| OV-00295 | Done | LOW | Low | [MOTION] `b`/`B` now skip leading whitespace and whitespace-only lines while searching for the previous word, while still treating genuinely empty lines as word boundaries. Behavior was verified against Neovim 0.12.4 and covered for both motions. (ovim-core/src/editor/motions/word.rs, ovim/tests/motion_bounds_test.rs) |
| OV-00296 | Done | LOW | Low | [EDIT] `{count}r{char}` now leaves the cursor on the final replaced grapheme and aborts without editing when the count exceeds the remaining line, matching Neovim 0.12.4. Updated count, end-of-line, cursor, undo/redo, and dot-repeat expectations cover the corrected semantics. (ovim-core/src/buffer/text_ops.rs, ovim/tests/replace_command_test.rs, ovim/tests/replace_mode_test.rs) |
| OV-00297 | Pending | LOW | Low | [TEXTOBJ] Counts silently discarded for all text objects — `editor.clear_count()` before resolving (ovim-core/src/editor/input/normal/text_objects.rs:33); `d2aw` deletes one word (vim: two). |
| OV-00298 | Pending | HIGH | High | [ARCH] File-mode and session-mode CLI ops are two independent implementations of one contract — `subcommands.rs` (cmd_edit_file/cmd_insert_file/cmd_delete_lines_file) vs `event_loop.rs` (handle_edit_line/handle_insert_lines/handle_delete_lines). This duplication produced OV-00279/280/284/285 (guards and semantics present in one copy, missing in the other). Extract a shared edit engine (find/validate/replace + insert + delete on text with uniqueness/ambiguity rules, escape expansion, line-ending policy) that both modes call; mode-specific code shrinks to I/O (read file vs buffer) and response formatting. |
| OV-00299 | Done | MEDIUM | Medium | [UNICODE] Complete the coordinate-space migration where documented approximations remained. **Fixed:** block paste (both branches), charwise paste after/before position + cursor landing, visual charwise/block yank extraction, visual charwise/block delete, charwise operator ranges (f/t/F/T application), y0/yh/y^ ranges, and yank-flash conversions now convert grapheme↔char against the live line text (ovim-core input/helpers.rs, char_motion.rs, normal/operators.rs, normal/text_objects.rs). Regression suite ovim/tests/unicode_coordinate_test.rs uses combining-mark clusters (1 grapheme, 2 chars) to pin each path. **Remaining:** replace_mode.rs still indexes by grapheme-as-char (needs per-grapheme old-text state for backspace-restore, a small behavioral rework — split to OV-00305). |
| OV-00300 | Pending | MEDIUM | High | [ARCH] Operator+motion combinations are hand-rolled per keybinding, so each drifts independently (source of OV-00288/289/290/292/293). **Progress:** introduced `ovim-core/src/motion_range.rs` — `MotionRange` (start, exclusive end, charwise/linewise) with `from_exclusive` encoding vim's exclusive-motion adjustments, plus `Buffer::delete_motion_range`/`yank_motion_range`; paragraph operators (d}/d{/y}/y{/c}/c{) migrated, fixing OV-00293. **Remaining:** migrate f/t/F/T (char_motion.rs OperatorRange), word/eol motions, and route new operators through it. |

| OV-00301 | Done | MEDIUM | Low | [TEST] Vim-semantics tests were written from the implementation, not the reference — 7 tests encoded bugs as expectations and passed for months. **Fixed:** convention documented in notes/TESTING_VIM_SEMANTICS.md (verify in `nvim --clean` before asserting, cite the reference in a comment, never update a failing semantics expectation without re-checking vim) and referenced from CLAUDE.md's Testing section. |

| OV-00302 | Pending | MEDIUM | Medium | [ARCH] Files exceeding the project's own 3k-line refactor threshold: agent_runtime/dispatch.rs (3.8k), ui/renderer/ai_chat.rs (3.4k), editor/ai_subagents.rs (3.0k). **Progress:** event_loop.rs (was 3.3k) split — API request dispatch, line-edit handlers, agent control plumbing, and snapshot builders moved to ovim/src/api_dispatch.rs (1.3k); event_loop.rs is now 1.9k and owns scheduling and terminal I/O only. Remaining three files still pending. |
| OV-00304 | Pending | LOW | Low | [TEST] Tests that `set_file_path("/tmp/...")` and then compare paths by string equality are host-state-dependent — `set_file_path` canonicalizes when the file EXISTS (macOS /tmp → /private/tmp), so a stray file on the machine flips the comparison. Bit us: `poll_pending_completion_response_shows_menu_on_fresh_result` failed deterministically once a stray /tmp/a.rs appeared (fixed by reading the effective path back). Latent in ~13 more sites (ovim-core/src/editor/lsp_modules/inlay_hints.rs, ai_tool_streaming.rs) using unlikely names like /tmp/Test.java. Sweep them to the effective-path pattern or a tempdir. |
| OV-00305 | Done | LOW | Medium | [UNICODE] Replace mode (`R`) indexed lines by grapheme col treated as char col and tracked one `char` of old text per replacement — replacing a multi-char grapheme (é as e+combining, ZWJ emoji) edited only its first scalar and backspace-restore lost the rest of the cluster. **Fixed:** replace/delete/restore boundaries now convert grapheme columns to rope char columns; original text is tracked as an aligned `Vec<Option<String>>`, distinguishing overwritten graphemes from EOL insertions; dot-repeat deletes target graphemes rather than scalar values. Regression coverage includes decomposed accents, ZWJ emoji, backspace restore, overwrite-vs-append behavior, undo, and dot-repeat. (ovim-core/src/editor/input/replace_mode.rs, ovim-core/src/repeat_action.rs, ovim/tests/grapheme_edit_bughunt_test.rs) |
| OV-00303 | Done | LOW | Medium | [ERROR] ~341 `let _ =` discards outside tests audited by category: log/flush/stdout writes, cleanup (kill/abort/remove/delete), channel sends on shutdown paths, and intentional unused-binding discards are all legitimately ignorable. The user-facing ones were the lua config/plugin/source command paths (fixed under OV-00197) and the workflow reload leftover (OV-00247). No other must-surface discards found. |
| OV-00306 | Done | HIGH | Low | [SYNTAX] Groovy/Gradle files had zero syntax highlighting — `groovy.scm` used `"this"`/`"trait"` as anonymous string tokens but `tree-sitter-groovy` models `this` as a named node and has no `trait` token at all, so `Query::new` always errored. `SyntaxHighlighter::new` returning `Err` was silently swallowed at both call sites (`frontend/loading.rs`, `frontend/tick.rs` do `if let Ok(...)` with no else), so nothing ever surfaced the failure. **Fixed:** removed `"trait"`, moved `this` to its own `(this) @keyword` pattern (matches java.scm's convention). Regression test added: `test_groovy_highlighter_parses_gradle_build_script_syntax` (ovim-core/src/syntax/highlighter.rs). **Follow-up filed separately (OV-00307):** the vendored grammar itself has a deeper bug — empty single-quoted strings (`''`) can't be tokenized at all (`character_literal`'s `REPEAT1` requires 1+ chars), which throws large swaths of real-world gradle files into unrecoverable `ERROR` nodes and blacks out highlighting for everything downstream until the parser happens to resync. |
| OV-00307 | Done | MEDIUM | Medium | [SYNTAX] Vendored `tree-sitter-groovy` 0.1.2's `character_literal` token rule was `' + REPEAT1(char) + '` (grammar.json) — REPEAT1 requires at least one character, so it had no rule for the empty string `''`, an extremely common Groovy/Gradle idiom (e.g. `mavenUrl = project.hasProperty(...) ? ... : ''`). Confirmed by direct reproduction (not inference): dumping the parse tree of the real-world kafka build.gradle showed the first ERROR node starting at the exact byte of the `''` on line 86 and spanning ~94 lines until an unrelated later token resynced the parser; an isolated hypothesis test (`x = 'a'` clean, `x = ""` clean, `x = ''` errors, same ternary shape with `'z'` clean vs `''` errors) pinned the cause unambiguously. **Fixed:** cloned amaanq/tree-sitter-groovy @ 70efb0b (the exact commit crates.io 0.1.2 was published from) into `vendor/tree-sitter-groovy`, overrode `character_literal` in its `grammar.js` (repeat1 -> repeat) since Groovy reuses Java's single-char literal token to represent its own any-length single-quoted string, regenerated `parser.c` via tree-sitter-cli 0.26.11, trimmed the vendor tree to only what's needed to build (removed .git/node_modules/other-language bindings/tests, ~6.4MB left, mostly the generated parser.c state tables), and pointed `tree-sitter-groovy` at it via `[patch.crates-io]` in the workspace Cargo.toml. Verified via full revert-to-red / reinstate-to-green cycle. Regression test: `groovy_empty_single_quote_string_parses_without_error` (ovim-core/src/syntax/highlighter.rs). **Follow-up filed separately (OV-00308):** even with this fixed, the real kafka build.gradle still has ~180 unrelated ERROR nodes from other real-world Gradle DSL constructs this immature grammar doesn't handle. |
| OV-00308 | Done | MEDIUM | High | [SYNTAX] Real-world Gradle build scripts hit many `tree-sitter-groovy` 0.1.2 gaps unrelated to OV-00307's `''` bug — confirmed by parsing the actual kafka build.gradle (4401 lines) post-OV-00307-fix and finding ~165 remaining ERROR/MISSING nodes, several catastrophic (one span alone swallowed 3516 lines). Root-caused and fixed 5 distinct gaps in `vendor/tree-sitter-groovy/grammar.js`, each isolated with a minimal repro before patching: (1) paren-less multi-param closures `{ a, b -> stmt1 \n stmt2 }` and single typed params `{ Type name -> ... }` — the `closure` rule only modeled a single trailing lambda/expression after a statement repeat, never a leading `params ->` shared with multiple statements; added `bare_closure_parameters`. (2) paren-less calls (`println "x"`) only worked at top level or inside a closure, not inside a plain Java `block` (method/if/try/catch bodies) — `statement` never gained the `juxt_function_call` alternative that `_toplevel_statement`/`closure` already had. (3) Gradle's `task xyz(type: Foo) { ... }` shorthand — `_juxt_argument_list` couldn't take a nested call as an argument and `juxt_function_call` had no trailing-closure field; added narrow `juxt_nested_call` (deliberately not the general `method_invocation`, to avoid a wide generics/dot-chain conflict surface) plus an optional closure body. (4) `foo.bar(args)()` — invoking the Closure returned by a call — had no grammar path at all (Java has no such syntax); this single rare construct was the trigger for the 3516-line cascading span. Added `closure_invocation` on `primary_expression`. (5) `def foo(untyped1, untyped2) { ... }` — `formal_parameter` required an explicit type (Java-only assumption); made it optional. Verified via isolated repros for each (0 errors) plus measured real-file impact: max contiguous blacked-out region dropped from 3516 lines to 13, and total affected lines from effectively most of the file to ~10% (450/4401). Full revert-to-red / reinstate-to-green cycle done for all 5 together. Regression tests: `groovy_paren_less_multi_param_closure_parses_without_error`, `groovy_juxt_call_inside_plain_block_parses_without_error`, `groovy_task_declaration_shorthand_parses_without_error`, `groovy_closure_invocation_chaining_parses_without_error`, `groovy_untyped_function_parameters_parse_without_error` (ovim-core/src/syntax/highlighter.rs). **Follow-up filed separately (OV-00309):** Groovy's regex operators (`=~`, `==~`) and slashy-string `/pattern/` literals have no grammar support at all — confirmed present in the real file (line ~4381) and not attempted here. The remaining ~450 affected lines are otherwise a long tail of many distinct small, low-frequency constructs (no single dominant root cause found), each requiring its own isolated reproduce-and-patch cycle. |
| OV-00309 | Pending | LOW | High | [SYNTAX] `tree-sitter-groovy` 0.1.2 has no grammar support at all for Groovy's regex find/match operators (`=~`, `==~`) or slashy-string regex literals (`/pattern/`) — confirmed in the real kafka build.gradle around line 4381 (`mvContent =~ /${x}\(\d+,.../`). Neither the operator nor the token exists anywhere in the grammar, so this needs new grammar rules from scratch (a slashy-string token with its own escaping/interpolation rules, distinguished from the division operator by context, plus the two new binary operators) rather than a small rule tweak like OV-00307/OV-00308's fixes — meaningfully larger scope, deferred. |
| OV-00310 | Done | HIGH | Medium | [TABS] Tab titles go stale because the title is a cached string on `TabPage` that only 2 of the many buffer-switching paths update. Only `load_file_async`'s NEW-buffer path (ovim-core/src/editor/mod.rs:1785) and the `:tabnew <file>` create-missing-file path (ovim-core/src/commands.rs:790) call `update_current_tab_title()`. Stale-title paths: (a) `load_file_async`'s already-open early return (mod.rs:1741-1756) syncs the buffer index but not the title — this is the file-finder repro: picking a file that's already open in any buffer leaves the previous title on the tab (screenshot: tab bar shows "shorthands.rs" while statusline shows lib.rs); (b) `switch_to_buffer`/`next_buffer`/`prev_buffer` (ovim-core/src/editor/buffer_manager.rs:136,167,206) never touch the title, so `:bn`/`:bp`/`:b N`, `open_file()` (goto-definition same-tab path, AI open_file tool) all leave it stale; (c) `delete_current_buffer` (buffer_manager.rs:250) switches the displayed buffer without a title update. **Recommended fix:** delete the cached-title design — derive the title at read time in `get_tab_title()` (ovim-core/src/editor/tab_manager.rs:77) from the tab's buffer (`current_buffer_index` → `buffer.file_path()` → file_name, using `editor.current_buffer_index` for the active tab), keep an explicit-title override only if needed for scratch tabs (their buffers already carry `[Title]` as file_path, so likely not). This also obsoletes the numeric-title heuristic (OV-00313) and fixes `:tabs` listing (commands.rs:738) which reads the same stale `tab.title()`. **Fixed:** deleted the cached-title design entirely — `TabPage` no longer stores a title; `get_tab_title()` (ovim-core/src/editor/tab_manager.rs) derives it at read time from the tab's buffer (`buffer_id` → `file_path()` → file name; the active tab reads the editor's current buffer directly). `update_current_tab_title()` and `set_current_tab_title()` removed along with all callers; `:tabs` now goes through `get_tab_title()`. Scratch tabs need no override — their buffers carry `[Title]` as file_path. Regression tests: `opening_already_open_file_updates_tab_title` (the file-finder repro), `tab_title_follows_buffer_switch`, `scratch_tab_derives_bracket_title` (tab_manager.rs). |
| OV-00311 | Done | HIGH | Medium | [TABS] `TabPage.current_buffer_index` is a raw index into `Editor::buffers`, and every buffer-removal site shifts the Vec without fixing up other tabs' stored indices: `delete_current_buffer` (ovim-core/src/editor/buffer_manager.rs:262), AI chat scratch close (ovim-core/src/editor/ai_chat_scratch.rs:56), code-explanation buffer discard (ovim-core/src/editor/ai_code_explanation.rs:1154). After a removal, every tab whose stored index is > removed points one buffer off, and a tab pointing exactly at the removed index shows an unrelated buffer — switching tabs then restores the wrong file with the wrong (or differently wrong) title, which matches the "names AND order incorrect" symptom. Repro: tab1=a.rs (buf 0), tab2=b.rs (buf 1), tab3=c.rs (buf 2); in tab1 run `:bd` (removes buf 0); `gt` to tab2 now shows c.rs, tab3 clamps/points past the end. **Recommended fix:** store stable `BufferId` in `TabPage` instead of a Vec index (resolve via `find_buffer_index_by_id` on switch, fall back to 0/current), mirroring how AI code already tracks buffers by id. Alternative (weaker): fix up all tabs' indices at each `buffers.remove()` site. **Fixed:** `TabPage` now stores a stable `BufferId` (`buffer_id()`/`set_buffer_id()`) instead of a Vec index; tab switches resolve it via `find_buffer_index_by_id` and fall back to adopting the editor's current buffer if the id no longer exists (`restore_current_tab_buffer`). Buffer removals can shift `Editor::buffers` freely without repointing any tab. The lsp goto-in-new-tab path, which replaces the buffer in place with a fresh id, now re-syncs the tab (lsp_integration.rs). Regression test: `deleting_buffer_does_not_corrupt_other_tabs` (tab_manager.rs). |
| OV-00312 | Done | MEDIUM | Low | [TABS] New tabs are appended at the end of the tab list (`TabPageManager::new_tab`, ovim-core/src/editor/tabpage.rs:118-123) but vim inserts a new tab page immediately AFTER the current one (`:tabnew`, verified `nvim --clean`: from tab 1 of 3, `:tabnew` makes the new tab number 2, not 4). With several tabs open, a tab created from a middle tab lands at the far end — perceived as "tab order is incorrect". Fix: `tabs.insert(current_tab_index + 1, ...)` and set `current_tab_index += 1`; audit `close_tab`'s index arithmetic still holds. **Fixed:** `TabPageManager::new_tab()` inserts at `current_tab_index + 1` and moves focus there (tabpage.rs), matching vim (verified `nvim --clean`: 3 tabs, `:tabfirst`, `:tabnew` → tab 2 of 4; cited in test comment). `close_tab`'s index arithmetic audited — unaffected. Regression test: `new_tab_inserts_after_current_tab` (tab_manager.rs). |
| OV-00313 | Done | LOW | Low | [TABS] `get_tab_title()` heuristic (ovim-core/src/editor/tab_manager.rs:84) treats any stored title that is ≤2 chars and all-numeric as a legacy placeholder and renders `[No Name]` — so a file literally named `1`..`99` (also common: numbered patch/test fixture files) shows as `[No Name]` in the tab bar. The heuristic exists only because `TabPageManager::new()`/`new_tab(None)` seed numeric titles (tabpage.rs:71,119-120). Dissolves for free under OV-00310's derive-at-read-time fix; otherwise seed `"[No Name]"` instead of numbers and delete the heuristic. **Fixed:** dissolved by OV-00310 — titles derive from real file names, numeric placeholder titles no longer exist, heuristic deleted. Regression test: `numeric_filename_renders_as_its_own_title` (tab_manager.rs). |
| OV-00314 | Done | LOW | Low | [TABS] Tab bar width math uses byte length: `render_tab_bar` does `tab_text.len()` (ovim/src/ui/renderer/status_widgets.rs:70) and pads/truncates on that, so filenames with non-ASCII (e.g. `æøå.rs`, CJK, emoji) misreport their cell width — overflow calculation and separator layout drift, making tabs render misaligned/cut. Use unicode-width (`UnicodeWidthStr::width`) like the rest of the renderer. **Fixed:** `render_tab_bar` measures `UnicodeWidthStr::width()` for both per-tab widths and the trailing fill (ovim/src/ui/renderer/status_widgets.rs); the crate was already imported in the file. |
| OV-00315 | Done | MEDIUM | Low | [HEADLESS/EX] `ovim exec undo` and `ovim exec redo` returned `E492` even though undo/redo were available from normal mode and `exec` is documented as the Ex-command interface. **Fixed:** the standard command dispatcher now supports Vim's `:u[ndo]` and `:red[o]` forms, so interactive, Lua, API, MCP, and headless callers share the same behavior. Headless parity tests cover full names and abbreviations across a live buffer mutation. (ovim-core/src/commands.rs, ovim/tests/headless_exec_parity_test.rs) |
| OV-00316 | Done | LOW | Low | [RENDER] LSP manager list/detail geometry used UTF-8 byte length for language and server names, misaligning commands and drawing overlong separators for non-ASCII names. **Fixed:** list padding and detail separators now use terminal display width, with regression coverage for CJK and Greek text. (ovim/src/ui/renderer/lsp_manager.rs) |
| OV-00317 | Done | MEDIUM | Medium | [LSP/UX] Switching from an LSP-backed file to a file without a server left the previous file's status and fallback server badge visible (live repro: `app.js` → `README.md` still rendered `LSP: JavaScript ready`). New-buffer and already-open async paths also bypassed parts of the canonical switch cleanup, and file-scoped result vectors/counts survived switches. **Fixed:** file switches clear all file-scoped LSP status, diagnostics, hover/picker results, slots, and decorations while retaining the global running-server registry; renderer badges now require a server for the current buffer's language; add/delete/new-tab paths run the same cleanup and the already-open async path delegates to `switch_to_buffer`. Regression coverage verifies status/count cleanup, current-language filtering, retained running servers, and both new/existing buffer paths. Reproduced and re-verified through a live headless session in `~/Testing/demo-project`. (ovim-core/src/editor/lsp_integration.rs, buffer_manager.rs, tab_manager.rs, mod.rs; ovim/src/ui/renderer/status_widgets.rs) |
| OV-00318 | Done | CRITICAL | Medium | [SUBAGENTS] Failed terminalization is swallowed, wedging the dispatch forever. When `run_one_inner` errors, the recovery path calls `terminalize_locked` → `terminate_conservatively` (which must APPEND run-log events) and discards its error via `.map(\|(terminal, _sweep)\| terminal).ok().flatten()` (agent_runtime/supervisor.rs:665-667). On ENOSPC the append fails, the record stays Starting/Running for the process lifetime: `wait_for_idle` never idles (supervisor.rs:524-530), the slot permanently counts against `max_total_per_run` (supervisor.rs:915-917, 8 slots per policy.rs:29), and the parent never gets a handoff. Compounded by dispatch.rs:1056-1094: `finish_with_handoff_and_warnings` sets `pending_handoff_status` after the handoff append but before the terminal lifecycle append; if the second append fails (`TerminalAfterHandoffFailed`), every retry is refused via `DispatchError::PendingHandoff` (dispatch.rs:1044-1046) — in-process unrecoverable; only restart rehydrate (dispatch.rs:580-597) repairs it. Fix: surface/retry terminalization failures (mirror the root turn's `pending_runtime_termination` retry in ai_run_events.rs:305-331) and make the pending-handoff completion retryable in-process. **Fixed:** failed terminalizations are parked in `SupervisorInner::pending_terminalizations` and retried from `settle_pending_terminalizations()` (called on dispatch allocate, drain_ready, and each wait_for_idle iteration), with the parent notified on settle; `ScheduledAgent.pending_handoff` now retains the durable handoff envelope, and `complete_pending_handoff` / `terminate_conservatively` resume the terminal lifecycle transition in-process instead of refusing with `PendingHandoff`. Regression tests: `interrupted_terminal_append_resumes_from_durable_handoff` (dispatch.rs), `failed_terminalization_is_retried_after_sink_heals` (supervisor.rs), both driven by an ENOSPC-injecting `FlakySink` (agent_runtime/test_support.rs). |
| OV-00319 | Done | HIGH | Medium | [SUBAGENTS] `AiSubagentService::run()` registers the run before its last fallible step, permanently poisoning config refresh. `runs.insert(run_id, ...)` at ovim-core/src/editor/ai_subagents.rs:457 precedes the fallible `supervisor.dispatches()?` at 458-465, so a failure there returns Err while the run stays registered; nothing ever removes entries from `runs` (no remove/retain/clear). `reconfigure_if_idle` (258-275) then refuses forever ("delegated-agent policy or provider profiles changed after a run started; restart Ovim") and after Lua config merge `ensure_compatible` (350-362) fails closed with "AI provider profiles changed after editor startup; restart Ovim before dispatching" — the whole delegated-agent surface (spawn/list/wait/send/follow-up) is dead for the session. Note the registration also happens from pure read paths (`headless_subagent_run` → `run()`, 1474-1480, reached from snapshot/repaint polling). Fix: only insert after all fallible steps, or remove on failure; consider evicting terminal runs. **Fixed:** the recovered-queued-children check now runs before `runs.insert`, so `run()` has no fallible step after registration; a failed open can no longer poison `reconfigure_if_idle`. (Terminal-run eviction deliberately not added — the compatibility guard is intended to persist across a completed run.) |
| OV-00320 | Pending | HIGH | Medium | [SUBAGENTS] Rehydrate writes on the open path, so an existing run cannot even be VIEWED while the disk is full: `AgentDispatchScheduler::rehydrate` appends sweep/completion events (ambiguous sweep dispatch.rs:633-639, pending-handoff completion 589-597, follow-up resume 607-615, route-drift sweep 667-669). ENOSPC → rehydrate fails → `AiSubagentService::run()` fails → agent tree, snapshot, and every control fail for that chat. Also any of ~12 `DispatchError::InvalidHistory` variants (dispatch.rs:421-689) makes the run permanently unopenable with no degraded read-only mode. Related: the user's escape hatch is also write-first — `operator_subagent_root` appends an `agent_operator_control` event BEFORE validating the target (ai_subagents.rs:2191-2211), so interrupt is the first thing to break on a full disk; and `wait_agent` livelocks because `AgentMailbox::consume` appends (mailbox.rs:528-545) — the same update is returned and re-failed forever (ai_subagents.rs:2013-2018). Fix: read-only rehydrate mode (defer sweeps until first write), validate-before-append in operator controls, and make mailbox consume idempotent/retryable. **Progress:** operator controls now validate the target agent against the registered run BEFORE appending the audit event (no residue for bad targets), and the interrupt's audit append is best-effort — cancellation of a live child is write-free and its terminal recording lands in OV-00318's parked retry, so interrupt works on a full disk. Remaining: read-only rehydrate mode and idempotent mailbox consume. |
| OV-00321 | Pending | HIGH | High | [SUBAGENTS] A panic in the child loop leaves the agent "live" forever with no escape hatch: everything after `run_one_inner` — `live.remove` (supervisor.rs:672), `notify_waiters` (683), re-drain spawn (684) — is unwind-unprotected. After a panic the AgentId stays in `inner.live`: `wait_for_idle` never returns, `followup_agent` refuses with `FollowupTargetBusy` (403-405), and `interrupt()` only cancels the already-dead task (471-473) — the user cannot interrupt out. `LiveAgentHandle::abort_handle` (79, set at 618) is stored but never used. Queued siblings stall (no re-drain). Fix: wrap the post-run bookkeeping in a guard (or catch_unwind / JoinHandle watchdog) so live-set removal, notification, and re-drain always run; use abort_handle as a forced-kill path. |
| OV-00323 | Done | HIGH | Low | [RENDER] EOL diagnostics rendered right-aligned at the screen edge (capped to 1/3 width) instead of next to their code in the DEFAULT config: `split_line_into_rows` pads every soft-wrap row to `text_width`, and `place_eol_on_line` measured the padded width, so every short line looked "full" and took the `overlay_eol_decoration_at_edge` branch. Centered-mode tests passed because that mode takes the AtBoxEdge branch first; there was no non-centered wrap test. **Fixed:** placement now measures `content_display_width` (trailing default-style space spans ignored); short lines get the diagnostic 2 cols after the code with the full remaining row available (the width cap from b41ef49 was removed — it targeted the wrong path and hid message text). Regression tests: `test_padded_wrapped_row_gets_adjacent_eol_not_edge_overlay`, `test_full_content_row_still_overlays_at_edge` (buffer.rs). Also: hover (`K`) with no LSP hover but diagnostics on the cursor line now opens the diagnostic float instead of setting a lingering "No hover info available" status (which also suppressed the message-line echo). |
| OV-00324 | Done | CRITICAL | Medium | [LSP] Externally modified files (git checkout, AI tools, other editors) desynced the LSP server FOREVER: the reload path's `mark_buffer_modified_force_send` cleared `last_flushed_content`, but `reconcile_document_sync_with_manager` treats None as "unknown, assume in sync" and re-seeded it with the CURRENT buffer content, so the no-op guard in `send_lsp_changes_if_modified` compared equal and dropped the didChange. The server kept analyzing the pre-reload text and re-publishing its stale diagnostics — live repro: valid file + external `let x: u32 = "wrong"` overwrite → buffer reloads, 0 diagnostics ever; conversely stale "Syntax Error" diagnostics persisted on valid code while `cargo` was clean (the datastructures repro). **Fixed:** `DocumentSyncState.force_full_resend` flag — set on forced resend, bypasses reconcile seeding and both no-op guards, sends a full-document didChange (no incremental baseline), cleared after queueing; the reload path also sends didSave so flycheck servers (rust-analyzer cargo/clippy) re-check the reloaded text. Verified live in both directions with a headless session. (ovim-core/src/editor/lsp_integration.rs, lsp_state.rs; ovim/src/frontend/refresh.rs) |
| OV-00325 | Done | HIGH | Low | [LSP] rust-analyzer initializationOptions used the pre-2023 `checkOnSave: { command, extraArgs }` shape: current rust-analyzer rejects it ("invalid config value: /checkOnSave: invalid type: map, expected a boolean", visible in lsp.log) and silently falls back to defaults; older versions ran clippy with `-D warnings`, promoting EVERY warning to an error — the editor showed errors where `cargo build`/`cargo test` was clean. **Fixed:** `checkOnSave: true` + `check.command = "clippy"` (modern shape), and no `-D warnings` so severities match cargo. (ovim-core/src/lsp/server.rs) |
| OV-00322 | Pending | MEDIUM | Medium | [SUBAGENTS] Disk-full second-order issues: (a) failed atomic writes leak temp files with no GC — `let _ = fs::remove_file` in registries (ai_subagents.rs:2898,2963) and artifact staging (run_log/artifact.rs:258,271,276), so failed writes under disk pressure consume MORE disk; (b) `DelegatedAgentWorkspacePolicy::minimum_free_space_mb` (2048MB, policy.rs:73) is dead code for the editor's read-only snapshot flow — the only enforcement lives in agent_runtime/workspace_layout.rs:960-975 and is never called outside that module's tests, so no free-space preflight exists anywhere on the spawn path; (c) run-wide budget counters are monotonic in-process but reset on restart (loop_runner.rs:522-562, supervisor.rs:183-186) — failed children permanently consume "provider-event budget exhausted" headroom until restart, and the "durable" cap silently isn't; (d) `parent_tools().unwrap_or_default()` (ai_subagents.rs:1739) silently removes all delegation tools on catalog error — indistinguishable from the feature being off; (e) UI-initiated interrupt/follow-up failures are only `log_warn!`ed while the status line claims the interrupt was requested (ai_subagents.rs:1435-1453); (f) composer input is cleared before the fallible send (1363-1418), destroying the user's typed message on error; (g) failed agents hold their `task_name` forever (`DuplicateTaskName`, dispatch.rs:1474-1480), blocking same-name retry. |
| OV-00326 | Done | CRITICAL | High | [LSP] Editing while the LSP was in use corrupted the server's copy of the document — root cause of "buffer shows valid code, diagnostics show garbage syntax errors" (live repro: buffer `x * "oops"`, rust-analyzer reporting `unknown literal prefix` from a phantom `2"oops"`). Three interlocking defects in the didChange sync engine: (a) **flush/did_change race orphaned edits** — `flush_pending_changes[_broadcast]` REMOVED the debouncer entry, but `did_change` had already cloned the Arc out of the map; when the timer-spawned flush task won the mutex, it sent the old text and dropped the entry, and the new edit was written into an orphaned debouncer whose timer flushed nothing. The editor then wedged (`target > sent` forever, queued-content guard blocks resend, diagnostics refresh suppressed). (b) **flush fallback poisoned the diff baseline** — when a hover/completion-triggered flush returned `Ok(None)` (nothing pending, e.g. after the race above, or after a failed/timed-out send that DISCARDED the payload), `ensure_lsp_document_synced` recorded the CURRENT buffer as `last_flushed_content`; the next incremental diff was computed against text the server never received, and the server applied it as a range edit over different content — permanent divergence. (c) **flush failures discarded the edit** with no retry. **Fixed** by restructuring: debouncer entries are persistent (didOpen..didClose), flush takes the pending payload under the mutex but never removes the entry; the manager keeps `server_texts: (server_id, uri) → exact text last delivered` (recorded at didOpen and each successful didChange send) and computes incremental diffs ONLY against that — editor-supplied `old_text` baselines are ignored (`None` still means "drop baselines, force full resend", which also un-defeats OV-00324: previously a pending debouncer's retained `old_text` silently downgraded a forced full resend to an incremental diff). Flushes are serialized per-uri (`flush_gates`) so versions can never be delivered out of order; failed/partial flushes re-arm the pending payload for timer-driven retry and return `Err`; the editor fallback no longer marks unflushed content as flushed; per-server baselines are cleared on server stop/replace. Regression tests: `flush_keeps_debouncer_entry_and_pending_coalesces`, `did_change_without_baseline_drops_server_texts`, and `incremental_flush_stream_reconstructs_editor_content` (fake LSP server process captures the didOpen/didChange frame stream; replaying it must reproduce the editor's final text even when the editor supplies stale baselines mid-stream — fails on the old code). Verified live against rust-analyzer in a headless session. (ovim-core/src/lsp/mod.rs, notifications.rs, server.rs; ovim-core/src/editor/lsp_integration.rs) |
| OV-00327 | Done | CRITICAL | Medium | [LSP] Buffer-mutating LSP results were applied with no staleness guard: `FormatResult`/`RenameResult`/`CodeActionsResult`/`OrganizeImportsResult` carried no file/version stamp, and their poll handlers (15s window) spliced UTF-16 ranges computed against the request-time snapshot into whatever the buffer had become — typing during a slow format/rename reverted or garbled the typed text. Completion accept had the same hole: while the menu is open the items are only re-filtered locally, so accepting ~100ms after typing applied the previous response's `textEdit.range` verbatim (e.g. `self.fo` → type `o` → accept → `self.foo_bar()o`). **Fixed:** all four mutating result types are stamped with `file_path` + `buffer_version` at fire time and discarded at poll time if the buffer moved on (status explains: "…discarded: buffer changed (rerun)"); `CompletionMenu` records `items_buffer_version`, and accept falls back to trigger-prefix replacement (and skips stale `additionalTextEdits`) when the version no longer matches. Regression tests: `stale_format_result_is_discarded`, `current_format_result_applies`, `stale_completion_accept_falls_back_to_prefix_replacement` (lsp_integration.rs). Remaining related gaps filed separately: OV-00330 (server-initiated applyEdit), OV-00331 (multi-file rename persistence), OV-00332 (same-position edit order). |
| OV-00328 | Done | HIGH | Medium | [LSP/RENDER] Split-brain diagnostic rendering after line-shifting edits: eol virtual-text decorations were projected through the edit log every frame, but the underline squiggle, gutter sign, message-line echo, and `<space>e` float read `diagnostics_by_line` by the diagnostic's ORIGINAL line with no remapping — after opening a line above an error, the message moved while the squiggle/sign/echo stayed on the old line for the whole typing burst + debounce + server latency. **Fixed:** diagnostics are anchored to rope char offsets when published (`DiagnosticAnchors`, lsp_state.rs) and `diagnostics_for_line`/`has_diagnostics_on_line`/`diagnostic_at_cursor` project them through the SAME `project_offset` edit-log replay the eol decorations use (`ProjectedDiagnostics`, per-frame snapshot in the renderer), so both pipelines agree on the line by construction; range columns clamp so a delete eating the span can't hide the gutter sign. Fallback when edit-log history is evicted matches the decorations' raw-position fallback, staying mutually consistent. Regression tests: `diagnostics_for_line_projects_through_line_shifting_edits`, `diagnostics_for_line_projects_columns_within_line` (verified failing pre-fix). (ovim-core/src/editor/lsp_modules/diagnostics.rs, lsp_state.rs, editor/mod.rs; ovim/src/ui/renderer/buffer.rs)
| OV-00329 | Done | MEDIUM | Low | [RENDER] Line render cache served stale/missing diagnostic squiggles: the underline is baked into cached rows but the cache key only fingerprinted the line's single best-severity EOL decoration, so a republish that changed the line's diagnostic SET without a buffer edit (e.g. save → cargo-check adds a second span, top message unchanged) hit the cache and never rendered the new squiggle until an edit/scroll/resize. **Fixed:** `line_decoration_cache_hash()` folds `ProjectedDiagnostics::line_hash` (full projected set: ranges + severities) into the cache key. Regression tests: `projected_line_hash_changes_when_set_changes_without_edit`, `test_diagnostic_republish_without_edit_changes_line_cache_key` (the latter asserts the pre-fix key collides while the new key misses). (ovim/src/ui/renderer/buffer.rs, line_cache.rs)
| OV-00330 | Pending | HIGH | Medium | [LSP] Server-initiated `workspace/applyEdit` is acked `applied: true` immediately on QUEUEING (notifications.rs) before the editor applies it on a later tick, and the actual outcome is never reported back — the server permanently believes the edit landed even on failure. **Progress:** the version-guard leg is done — `OptionalVersionedTextDocumentIdentifier.version` is threaded to apply time and a mismatched version skips that document's edits with a status message (`stale_versioned_edit_is_discarded_with_status`, `matching_and_unversioned_edits_still_apply`); the guard compares the current file's lsp version (other files accepted unchecked — the apply path is sync, `get_document_version` is async; documented in code). Remaining: respond to the request only after the editor applies (thread the response through the tick) and report apply failures honestly.
| OV-00331 | Done | HIGH | Medium | [LSP] Multi-file WorkspaceEdit (rename across files) silently lost edits: non-open files were loaded into hidden in-memory buffers, edited, and never written to disk; `Editor::is_modified()` checked only the current buffer so `:qa` discarded them without warning; `:wa` had no handler. **Fixed:** (a) hidden buffers that were CLEAN before a workspace edit are written straight to disk as part of the apply (synchronous fs io mirroring `save_as_async`: line endings, encoding, mtime refresh); buffers with pre-existing user modifications are not auto-saved — the quit guard covers them; (b) `:wa`/`:wall` (+`!`) writes every modified named non-scratch buffer, continuing past per-buffer errors (E141 unnamed; E45 readonly; repo's E211 external-change guard kept per `:w` philosophy, `!` overrides); (c) `:qa` and last-window `:q` refuse when ANY non-scratch buffer is modified (`any_buffer_modified()`). Vim semantics (E37/E141/E45 flows) verified `nvim --clean`, cited in test comments. Regression tests: `workspace_edit_writes_hidden_buffer_to_disk`, `workspace_edit_leaves_current_buffer_unwritten`, `workspace_edit_skips_write_through_for_user_modified_hidden_buffer`, `qa_refuses_when_a_non_current_buffer_is_modified`, `q_on_last_window_refuses_when_a_hidden_buffer_is_modified`, `wa_writes_all_named_modified_buffers`, `wa_reports_e141_for_unnamed_buffer_but_still_writes_named`. Known gap (in scope of a follow-up): `:wq` with hidden modified buffers still quits after saving the current buffer. (ovim-core/src/editor/buffer_manager.rs, lsp_modules/workspace_edits.rs, commands.rs, change_tracking.rs, input/commands.rs)
| OV-00332 | Done | MEDIUM | Low | [LSP] TextEdit application order violated the LSP spec for same-position edits (descending sort applied top-first reversed same-position inserts; per-edit UTF-16→char conversion against the already-mutated rope let a sibling replace consume an insert's text), and out-of-bounds edits silently no-opped while the apply reported success. **Fixed:** single shared `apply_text_edits_to_buffer` resolves ALL ranges against the pre-edit snapshot, applies bottom-to-top with same-position ordering that preserves spec array order (replaces consume original text first; inserts in reverse array order), and counts dropped out-of-bounds edits as failure (inserts may target the phantom EOF line; end positions clamp, per spec). Both former sort copies delegate to it. Regression tests: `same_position_inserts_apply_in_array_order`, `insert_at_sibling_replace_start_is_not_consumed`, `out_of_bounds_edit_reports_failure`. (ovim-core/src/editor/buffer_manager.rs, lsp_modules/workspace_edits.rs)
| OV-00333 | Done | HIGH | Medium | [LSP/UI] A wedged-but-alive language server froze the editor: `did_save_broadcast`/`did_open_broadcast`/`did_close_broadcast` (and the single-server variants) called `server.notify` with NO timeout, and the outgoing channel (bounded 100) blocks when the server stops reading stdin — `:w` on a stopped server (e.g. `kill -STOP rust-analyzer`) hung the UI event loop indefinitely (flushes have a 5s timeout per server, but these notifies had none, and both run inline on the tick task inside the input `select!`). **Fixed:** all six lifecycle notify sites route through `notify_with_timeout` (5s hard ceiling); a wedged server now degrades LSP with a logged error instead of freezing the editor. Regression test `notify_times_out_when_server_stops_reading` (spawns a real never-reads-stdin process, saturates the pipe + channel, asserts prompt error). Follow-up (still open, folded into this row): moving LSP sync off the input/render select entirely so even the 5s worst case can't stall a tick. (ovim-core/src/lsp/notifications.rs) |
| OV-00334 | Done | MEDIUM | Medium | [LSP] `:w <newpath>` onto a document the server already had open desynced it: `handle_file_path_transition_after_save` removed the new path's editor-side sync entry, and the next reconcile seeded `did_open_sent=true` + `last_flushed_content=current buffer` because the manager still had the target file claimed (`sent_version > 0`) — but the server held the TARGET file's old text, and the first edit after save-as never reached the server. **Fixed:** the transition now resets the target path's sync entry with `buffer_modified=true` + `force_full_resend=true`, so reconcile skips the false seed and the very next sync tick pushes the full buffer (heals immediately, no user edit required; brand-new paths still take the normal didOpen route unaffected). (ovim-core/src/editor/lsp_integration.rs) |
| OV-00339 | Pending | LOW | Low | [LSP] `flush_pending_changes_broadcast` retries an undeliverable pending change every 150ms indefinitely while no server is registered for the language (each retry logs a warn to lsp.log). Self-heals when a server appears; bounded cost; consider exponential backoff or capping retries with a terminal state if server registration never happens. |
| OV-00340 | Done | HIGH | Low | [UNDO] User-reported: edit a file, undo back to the saved state — the buffer still showed as modified and `:q` refused, forcing `:q!`. Root cause: `Editor::is_modified()` ORed the sticky `Buffer.modified` bool (set by every mutation INCLUDING undo's own, cleared only on save/load) with the save-point check, so undo could never clear it. **Fixed:** after undo/redo, the sticky bool is cleared when the change manager lands exactly on the save point (`sync_modified_flag_with_save_point`, change_tracking.rs) — safe because OV-00341 made the save point a state identity and bypass mutations clear it. Vim semantics verified `nvim --clean` (edit+`u`→&modified==0; below save→1; redo-to-save→0; divergent same-depth→1), cited in the test file. Regression tests (verified failing pre-fix in a worktree at the previous HEAD): `undo_back_to_start_clears_modified`, `undo_back_to_save_point_clears_modified`, `undo_below_save_point_marks_modified_and_redo_clears`, `divergent_edit_at_same_undo_depth_stays_modified` (ovim/tests/undo_modified_flag_test.rs).
| OV-00342 | Pending | LOW | Medium | [MOTION] `{count}\|` targets character/grapheme columns, not vim's virtual/screen columns (external review on OV-00338, verified in nvim: on `\tabc`, `3\|` stays on the tab; ovim lands on `b`). Wide and combining graphemes drift similarly. Fix needs the display-width machinery (screen-col → grapheme mapping) plus `nvim --clean` verification of tab/wide-char cases; deferred from the 1.2.5 release cut as non-blocking. (ovim-core/src/editor/input/normal/motions_input.rs) |
| OV-00343 | Pending | LOW | Medium | [BUFFERS] Scratch-buffer identity is a filename heuristic (`[Title]` basename, buffer_manager.rs `is_scratch_path`). Data-safety decisions (quit protection, `:wa`) now use `is_scratch_buffer` = bracket-name AND path does not exist on disk, which protects real on-disk files named like `[draft]` — but a brand-new never-written file literally named `[draft]` is still misclassified as scratch until first written, and the LSP didOpen/didClose gating still uses the bare name heuristic. Proper fix: an explicit `scratch` flag on `Buffer` set at every scratch creation site (AI chat, code explanation, walkthrough snapshots, pickers), with the heuristics deleted. (external review on OV-00331) |
| OV-00344 | Done | HIGH | Medium | [UNICODE] Emoji width mismatch between cursor math and rendered cells — ratatui 0.29 renders with unicode-width 0.2 (VS16 sequences like ❤️ and ZWJ sequences render as one 2-column glyph) while ovim's display math summed per-char widths with unicode-width 0.1, so the hardware cursor landed short of (VS16) or past (ZWJ) the glyph, and wrap points / horizontal slicing / highlight shifting disagreed with rendered cells. Fixed by upgrading both crates to unicode-width 0.2, adding `grapheme_display_width`, and converting all display-column walks to grapheme iteration (display.rs, wrap.rs, wrap_map.rs, renderer buffer.rs/helpers.rs). Follow-up sweep converted the chat panel (input wrap, selection, highlight), picker rows, markdown code blocks (also fixed byte-vs-char highlight range indexing), conversation tree previews, and statusline padding to width-based grapheme-safe truncation; fixed a byte-slice panic on non-ascii git branch names in the margin statusline. Regression tests render to a TestBackend and assert the cursor lands on the rendered glyph (unwrapped + wrapped). |
| OV-00345 | Done | HIGH | Medium | [LSP/DIAG] Multiline diagnostics were only reachable and underlined on their start line. Cursor feedback, hover fallback, code-action context and rendering now share half-open coverage rules; gutter/EOL markers remain on the start line. Continuation underlines invalidate cached rows when endpoints or severity change, project through edits/undo, and preserve whole graphemes through tabs, wrapping and horizontal scrolling. Diagnostic payloads are stored once per projected snapshot with span-independent indices; reversed ranges are rejected and oversized fallback endpoints saturate. Fixed adjacent-endpoint selection, continuation quick-fix retry columns, stale status after closing a diagnostic float, and extra ANSI spaces after wide glyphs. Verified with refresh-path and rendered-cell regressions, real headless/stdio flows (including null hover, republish/clear and file switches), and an installed TypeScript server. |
| OV-00341 | Done | HIGH | Medium | [UNDO] `ChangeManager.save_point` tracked undo-stack LENGTH, not state identity — "save, undo, make a different edit" landed at the same depth and `is_at_save_point()` returned true while the buffer differed from disk (masked by the sticky bool OV-00340 removed from the equation; would otherwise be silent data loss on `:q`). Same confusion in `ChangeToken`/`pop_by_token`: an index-based token could pop a DIFFERENT change after undo + new edit (ABA) — tokens are held across pending visual-block change/delete flows. **Fixed:** every stack entry is an `UndoEntry { change, seq }` with a monotonically increasing sequence number that travels with the entry between the undo/redo stacks; the save point stores the seq of the stack top at save (0 = empty); `is_at_save_point` compares top seq; `ChangeToken` carries the seq and `pop_by_token` matches on it; `fixup_after_bypass_mutation` clears the save point (untracked edits make stack states unidentifiable with disk). Regression tests: `divergent_edit_at_same_depth_is_not_at_save_point`, `save_point_survives_undo_redo_rotation`, `pop_by_token_rejects_different_change_at_same_index`, `pop_by_token_pops_matching_top` (ovim-core/src/change.rs).
| OV-00335 | Done | LOW | Low | [LSP] Wrong-file diagnostic refresh results stamped `current_file_lsp_version`/`current_file_lsp_sent_version` BEFORE the deferred and wrong-file guards, briefly poisoning the new file's version fields after a switch (spurious inlay-hint drops). **Fixed:** stamps moved below both guards; deferred results no longer stamp at all (next non-deferred refresh restores them). Regression tests: `poll_wrong_file_result_does_not_stamp_version_fields`, updated `poll_pending_diagnostic_refresh_response_requeues_deferred_result`. (ovim-core/src/editor/lsp_modules/diagnostics.rs)
| OV-00336 | Done | LOW | Low | [LSP] Unversioned CLEARING diagnostics publications were hard-dropped when unsent edits existed (`last_sent < current_version` branch, ovim-core/src/lsp/mod.rs) while the sibling settle-window branch defers them precisely so "the server's only clearing update" isn't lost. A save-only publisher emitting an empty set inside the 150ms debounce window of the next keystroke lost the clear, and old errors kept rendering until the next save. **Fixed:** the unsent-edits branch now defers the publication exactly like the settle branch (150ms re-evaluation window; `apply_deferred_diagnostics` discards it if the document version moved on, and a fresh publication overwrites it). |
| OV-00337 | Done | LOW | Low | [HEADLESS/KEYS] `ovim send` special-key notation was case-sensitive: `<esc>`, `<cr>`, `<tab>` were inserted as literal text (only `<Esc>`/`<CR>`/`<Tab>` parsed), unlike vim's case-insensitive notation — a failed `<esc>` left the session in insert mode and every subsequent keystroke landed as buffer text. **Fixed:** modifier prefixes and multi-char key names now match case-insensitively (verified `nvim --clean`: `:map <esc>`/`<c-w>`/`<cr>` register as their canonical spellings); single-char base keys stay case-sensitive because `<C-a>` and `<C-A>` are distinct chords; `<f1>`-style function keys also accepted. Regression test `special_key_notation_is_case_insensitive_for_names_and_modifiers` (ovim/tests/frontend_api.rs). (ovim/src/api/state.rs) |
| OV-00338 | Done | LOW | Low | [MOTION] `{count}\|` (go to column N) was a no-op — `19\|` left the cursor in place while vim moves to column 19. **Fixed:** implemented in normal-mode motions (ovim-core/src/editor/input/normal/motions_input.rs): moves to column `count` (default 1), clamped to the last character; desired-column updated. Semantics verified in `nvim --clean` on a 26-char line (`19\|` → col 19, `99\|` → col 26, bare `\|` → col 1; cited in test comments). Regression tests: `test_bar_moves_to_count_column`, `test_bar_without_count_moves_to_column_one`, `test_bar_count_past_line_end_clamps_to_last_char` (ovim/tests/motion_bounds_test.rs). |


## Bugs Filed Against Hyperion (if any)

These may belong in Hyperion's tracker after investigation:

| ID | Status | Priority | Description |
|----|--------|----------|-------------|
| HY-TRIAGE-01 | Triage | UNKNOWN | Method-call hover needs a live Hyperion reproduction. OV-00345 verifies ovim's diagnostic fallback when a server returns no hover; diagnostic text from `K` alone does not establish whether Hyperion supplied symbol information. |
| HY-TRIAGE-02 | Triage | UNKNOWN | find-references returning empty - may be unimplemented in hyperion-lsp |
| HY-TRIAGE-03 | Triage | UNKNOWN | goto-definition fails on DTO type references (AdminSqlRequest) - possibly classpath/dependency resolution |

## Notes

- OV-00003 and OV-00004 are MCP vs LSP protocol mismatch - ovim uses MCP, some LSPs don't implement those tools
- HY-TRIAGE items need investigation to determine if they're hyperion-lsp gaps or ovim request/response issues
- Closed issues moved to [CLOSED_ISSUES.md](CLOSED_ISSUES.md)
